# 简历项目-在线问诊系统-核心模块-医保对接 Insurance

> 文档定位：在线问诊系统核心模块"医保对接 Insurance"的详细技术文档
> 适用场景：简历项目深挖、面试追问、技术评审参考
> 关联文档：架构文档 / 技术亮点与难点拆解 / 面试问答预演 / 核心模块-处方开具

---

## 一、模块定位与业务背景

### 1.1 模块定位

医保对接 Insurance 是互联网医院的**资金流出口模块**--处方开具后的药品费用，需要与医保系统结算，决定患者实际支付金额。这个模块的本质是**平台与医保局的资金契约**：

1. **目录契约**：平台药品库与医保目录的映射，决定哪些药能报、报多少
2. **结算契约**：每张处方的医保报销金额，与医保局系统的实时交互
3. **对账契约**：日终对账，确保平台记录与医保局记录一致

```
处方开具（已审核通过）
   ↓
医保预结算（开方时估算报销金额）
   ↓
医保实时结算（支付时调用医保局接口）
   ↓
患者自付支付（医保报销后的自付部分）
   ↓
日终对账（平台 vs 医保局）
   ↓
差错处理（短款补扣 / 长款退回）
```

### 1.2 业务规模与 SLA

| 指标 | 数值 | 说明 |
|------|------|------|
| 日均结算笔数 | 4.2w 笔 | 占处方量的 87%（部分处方患者放弃购药） |
| 峰值结算笔数 | 7.8w 笔/日 | 冬春疾病高发期 |
| 医保目录药品数 | 12.6w 种 | 含西药、中成药、中药饮片 |
| 医保覆盖省份 | 23 个 | 已对接 23 个省级医保平台 |
| 结算接口延迟 | < 2 秒 | 医保局接口 P99 |
| 结算成功率 | 99.92% | 含医保局接口失败 |
| 对账差异率 | < 0.05% | 平台与医保局金额差异 |
| 对账完成时间 | T+1 日 06:00 | 次日 6 点前完成对账 |

### 1.3 合规要求

1. **医保定点资质**：执业挂靠医院必须是医保定点医疗机构
2. **目录同步**：医保目录更新后 24 小时内同步到平台
3. **结算实时性**：患者支付前必须完成医保实时结算
4. **数据上报**：结算数据实时上报医保局，禁止延迟
5. **资金安全**：医保报销金额由医保局直接结算给医院，平台不参与资金流转
6. **隐私保护**：医保结算涉及患者身份证号、医保卡号，需加密存储
7. **对账必达**：每日对账，差异必须 24 小时内处理

### 1.4 我的角色

- 模块技术负责人（4 人小组：我 + 3 名中级）
- 主导医保对接架构设计，统一封装 23 个省级医保平台的接口差异
- 主导对账系统建设，将差异率从 0.3% 降到 0.05%
- 主导医保目录同步系统，实现 24 小时内全量同步

---

## 二、核心数据模型

### 2.1 医保目录表 MedicareCatalog（MySQL + ES）

```sql
CREATE TABLE `medicare_catalog` (
  `id`                bigint       NOT NULL AUTO_INCREMENT,
  `medicare_code`     varchar(32)  NOT NULL COMMENT '医保编码',
  `drug_code`         varchar(32)  NOT NULL COMMENT '平台药品编码',
  `drug_name`         varchar(128) NOT NULL,
  `spec`              varchar(64)  NOT NULL,
  `category`          tinyint      NOT NULL COMMENT '1甲类 2乙类 3丙类',
  `payment_ratio`     decimal(5,2) NOT NULL COMMENT '医保支付比例（%）',
  `ceiling_price`     decimal(10,2) NOT NULL COMMENT '医保支付限价',
  `province_code`     varchar(2)   NOT NULL COMMENT '省份编码',
  `effective_date`    date         NOT NULL,
  `expiry_date`       date         DEFAULT NULL,
  `restriction`       json         DEFAULT NULL COMMENT '使用限制（适应症/人群等）',
  `version`           int          NOT NULL DEFAULT 0,
  `update_time`       datetime     NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_medicare_province` (`medicare_code`, `province_code`),
  KEY `idx_drug_province` (`drug_code`, `province_code`),
  KEY `idx_province_category` (`province_code`, `category`)
) ENGINE=InnoDB COMMENT='医保目录表';
```

**关键设计**：
- 医保编码 + 省份唯一（同一药品在不同省份医保政策不同）
- `payment_ratio` 报销比例，甲类 100%、乙类 70-90%、丙类 0%
- `ceiling_price` 医保限价，超出部分患者自付
- `restriction` 使用限制 JSON（适应症、人群、疗程等）
- 全量同步到 ES，便于快速检索

### 2.2 医保结算单表 MedicareSettlement（MySQL）

```sql
CREATE TABLE `medicare_settlement` (
  `settle_id`         varchar(32)  NOT NULL COMMENT '结算单号',
  `rx_id`             varchar(32)  NOT NULL COMMENT '处方号',
  `order_id`          bigint       NOT NULL,
  `patient_id`        bigint       NOT NULL,
  `patient_card_no`   varchar(128) NOT NULL COMMENT '医保卡号（加密）',
  `patient_id_hash`   varchar(64)  NOT NULL COMMENT '身份证SHA256',
  `hospital_id`       bigint       NOT NULL,
  `province_code`     varchar(2)   NOT NULL,
  `total_amount`      decimal(10,2) NOT NULL COMMENT '处方总金额',
  `medicare_amount`   decimal(10,2) NOT NULL COMMENT '医保报销金额',
  `self_pay_amount`   decimal(10,2) NOT NULL COMMENT '患者自付金额',
  `detail_items`      json         NOT NULL COMMENT '明细（见2.3）',
  `settle_status`     tinyint      NOT NULL COMMENT '0预结算 1已结算 2已撤销 3已对账 4差异',
  `medicare_req_no`   varchar(64)  NOT NULL COMMENT '医保局请求号（幂等）',
  `medicare_resp_no`  varchar(64)  DEFAULT NULL COMMENT '医保局返回流水号',
  `pre_settle_time`   datetime     DEFAULT NULL COMMENT '预结算时间',
  `settle_time`       datetime     DEFAULT NULL COMMENT '实时结算时间',
  `revoke_time`       datetime     DEFAULT NULL,
  `reconcile_status`  tinyint      NOT NULL DEFAULT 0 COMMENT '0未对账 1已对账 2差异',
  `reconcile_time`    datetime     DEFAULT NULL,
  `version`           int          NOT NULL DEFAULT 0,
  `create_time`       datetime     NOT NULL,
  `update_time`       datetime     NOT NULL,
  PRIMARY KEY (`settle_id`),
  UNIQUE KEY `uk_rx` (`rx_id`),
  UNIQUE KEY `uk_medicare_req` (`medicare_req_no`),
  KEY `idx_patient_status` (`patient_id`, `settle_status`),
  KEY `idx_reconcile_status` (`reconcile_status`, `settle_time`),
  KEY `idx_province_time` (`province_code`, `settle_time`)
) ENGINE=InnoDB COMMENT='医保结算单表';
```

**关键设计**：
- `medicare_req_no` 医保局请求号，幂等关键（同一请求号重复调用返回相同结果）
- `patient_card_no` 加密存储（AES-256），`patient_id_hash` 用于查询
- `settle_status` 与 `reconcile_status` 分离，便于独立追踪
- `detail_items` 存每项药品的报销明细

### 2.3 结算明细 detail_items 结构

```json
[
  {
    "seq": 1,
    "drugCode": "H20203001",
    "drugName": "阿托伐他汀钙片",
    "medicareCode": "X2024A001",
    "category": "甲类",
    "quantity": 28,
    "unitPrice": 32.50,
    "totalPrice": 130.00,
    "ceilingPrice": 30.00,
    "paymentRatio": 100.00,
    "medicareAmount": 130.00,
    "selfPayAmount": 0.00,
    "overCeilingAmount": 0.00
  },
  {
    "seq": 2,
    "drugCode": "Z20210045",
    "drugName": "蓝芩口服液",
    "medicareCode": "Y2024B045",
    "category": "乙类",
    "quantity": 6,
    "unitPrice": 25.00,
    "totalPrice": 150.00,
    "ceilingPrice": 22.00,
    "paymentRatio": 80.00,
    "overCeilingAmount": 18.00,
    "medicareAmount": 105.60,
    "selfPayAmount": 44.40
  }
]
```

**关键计算**：
- `overCeilingAmount` = max(0, unitPrice - ceilingPrice) × quantity -- 超限价部分患者全自付
- `medicareAmount` = (unitPrice - overCeilingUnitPrice) × quantity × paymentRatio -- 医保报销金额
- `selfPayAmount` = totalPrice - medicareAmount -- 患者自付

### 2.4 对账记录表 ReconcileRecord（MySQL）

```sql
CREATE TABLE `reconcile_record` (
  `id`                bigint       NOT NULL AUTO_INCREMENT,
  `reconcile_date`    date         NOT NULL COMMENT '对账日期',
  `settle_id`         varchar(32)  NOT NULL,
  `rx_id`             varchar(32)  NOT NULL,
  `local_amount`      decimal(10,2) NOT NULL COMMENT '平台记录金额',
  `medicare_amount`   decimal(10,2) NOT NULL COMMENT '医保局记录金额',
  `diff_amount`       decimal(10,2) NOT NULL COMMENT '差异金额',
  `diff_type`         tinyint      NOT NULL COMMENT '0无差异 1短款 2长款 3缺失 4多余',
  `handle_status`     tinyint      NOT NULL DEFAULT 0 COMMENT '0未处理 1已处理 2已挂账',
  `handle_opinion`    varchar(512) DEFAULT NULL,
  `handle_time`       datetime     DEFAULT NULL,
  `create_time`       datetime     NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_date_settle` (`reconcile_date`, `settle_id`),
  KEY `idx_diff_type_status` (`diff_type`, `handle_status`)
) ENGINE=InnoDB COMMENT='对账记录表';
```

**差异类型**：
- `短款`：平台记录医保报销 100 元，医保局实际报销 95 元（平台少收 5 元）
- `长款`：平台记录医保报销 100 元，医保局实际报销 105 元（平台多收 5 元）
- `缺失`：平台有结算记录，医保局没有
- `多余`：医保局有结算记录，平台没有

### 2.5 医保局接口适配表 MedicareProvider（配置表）

```sql
CREATE TABLE `medicare_provider` (
  `province_code`     varchar(2)   NOT NULL,
  `provider_code`     varchar(32)  NOT NULL COMMENT '医保平台代号',
  `api_base_url`      varchar(256) NOT NULL,
  `app_id`            varchar(64)  NOT NULL,
  `app_secret_enc`    varchar(512) NOT NULL COMMENT '加密的密钥',
  `sign_algorithm`    varchar(16)  NOT NULL COMMENT 'SM2/RSA/MD5',
  `interface_version` varchar(16)  NOT NULL,
  `timeout_ms`        int          NOT NULL DEFAULT 3000,
  `retry_count`       int          NOT NULL DEFAULT 2,
  `enabled`           tinyint      NOT NULL DEFAULT 1,
  PRIMARY KEY (`province_code`)
) ENGINE=InnoDB COMMENT='医保局接口配置';
```

**关键设计**：
- 每个省份独立配置（接口规范、加密算法、超时时间都不同）
- `app_secret_enc` 加密存储（KMS 托管密钥）
- 支持动态启停（故障时关闭某省份结算）

---

## 三、核心业务流程

### 3.1 医保结算完整链路

```
处方审核通过 -> RxReviewedPassed 事件 -> Insurance服务
                                          │
                                          ├─ 1. 查询患者医保资质（医保局接口）
                                          ├─ 2. 查询医保目录（本地 ES）
                                          ├─ 3. 计算预结算金额
                                          │     ├─ 遍历处方药品
                                          │     ├─ 查医保目录（按省份）
                                          │     ├─ 计算每项报销金额
                                          │     └─ 汇总医保报销 + 自付金额
                                          ├─ 4. 保存预结算记录（status=0预结算）
                                          └─ 5. 推送给患者"去支付"

患者点击支付 -> Pay服务 -> Insurance服务（实时结算）
                                │
                                ├─ 6. 调用医保局实时结算接口
                                │     ├─ 入参：处方信息 + 患者医保信息 + 预结算金额
                                │     ├─ medicare_req_no 幂等控制
                                │     ├─ 超时 3 秒，重试 2 次
                                │     └─ 返回：medicare_resp_no + 实际报销金额
                                ├─ 7. 校验返回金额与预结算金额
                                │     ├─ 差异 < 1 元：接受，更新结算单
                                │     └─ 差异 >= 1 元：拒绝支付，提示患者重新预结算
                                ├─ 8. 更新结算单（status=1已结算）
                                └─ 9. 通知 Pay 服务实际自付金额

患者支付自付部分 -> Pay服务 -> Insurance服务
                                  │
                                  ├─ 10. 更新结算单（关联支付流水）
                                  └─ 11. 触发药品配送

日终对账（T+1 02:00）-> Insurance服务
                          │
                          ├─ 12. 拉取医保局当日结算流水
                          ├─ 13. 与本地结算单逐笔比对
                          ├─ 14. 生成对账记录（无差异 / 短款 / 长款 / 缺失 / 多余）
                          ├─ 15. 差异记录进入待处理队列
                          └─ 16. 监控告警：差异率 > 0.1% 触发 P1
```

### 3.2 医保预结算算法

```java
// 伪代码：医保预结算
public PreSettleResult preSettle(Rx rx, Patient patient) {
    // 1. 查询患者医保资质
    MedicareEligibility eligibility = medicareProviderService.queryEligibility(
        patient.getIdCard(), patient.getProvinceCode());
    if (!eligibility.isEligible()) {
        return PreSettleResult.notEligible();
    }
    
    // 2. 遍历处方药品，计算报销
    BigDecimal totalAmount = BigDecimal.ZERO;
    BigDecimal medicareAmount = BigDecimal.ZERO;
    List<SettleItem> items = new ArrayList<>();
    
    for (DrugItem drug : rx.getDrugItems()) {
        // 查医保目录（按省份）
        MedicareCatalog catalog = medicareCatalogService.findByDrugAndProvince(
            drug.getDrugCode(), patient.getProvinceCode());
        
        if (catalog == null) {
            // 不在医保目录，患者全自付
            items.add(SettleItem.fullSelfPay(drug));
        } else {
            // 计算超限价部分
            BigDecimal overCeiling = calculateOverCeiling(drug, catalog);
            // 计算医保报销金额
            BigDecimal medicarePay = calculateMedicarePay(drug, catalog, overCeiling);
            // 计算自付金额
            BigDecimal selfPay = drug.getTotalPrice().subtract(medicarePay);
            
            items.add(SettleItem.of(drug, catalog, medicarePay, selfPay, overCeiling));
            medicareAmount = medicareAmount.add(medicarePay);
        }
        totalAmount = totalAmount.add(drug.getTotalPrice());
    }
    
    // 3. 计算起付线与封顶线
    BigDecimal afterDeductible = applyDeductible(medicareAmount, eligibility);
    BigDecimal afterCap = applyCap(afterDeductible, eligibility);
    
    return PreSettleResult.builder()
        .totalAmount(totalAmount)
        .medicareAmount(afterCap)
        .selfPayAmount(totalAmount.subtract(afterCap))
        .items(items)
        .build();
}

// 超限价计算
private BigDecimal calculateOverCeiling(DrugItem drug, MedicareCatalog catalog) {
    BigDecimal overUnit = drug.getUnitPrice()
        .subtract(catalog.getCeilingPrice())
        .max(BigDecimal.ZERO);
    return overUnit.multiply(BigDecimal.valueOf(drug.getQuantity()));
}

// 医保报销计算
private BigDecimal calculateMedicarePay(DrugItem drug, MedicareCatalog catalog, 
                                         BigDecimal overCeiling) {
    BigDecimal withinCeiling = drug.getTotalPrice().subtract(overCeiling);
    return withinCeiling.multiply(catalog.getPaymentRatio())
        .divide(BigDecimal.valueOf(100), 2, RoundingMode.HALF_UP);
}
```

### 3.3 状态机：医保结算单生命周期

```
            ┌────────────┐
            │  预结算    │
            │ PRE_SETTLE │
            │    (0)     │
            └─────┬──────┘
                  │ 实时结算成功
                  ▼
            ┌────────────┐
            │  已结算    │
            │  SETTLED   │
            │    (1)     │
            └─────┬──────┘
                  │ T+1 对账
                  ▼
            ┌────────────┐
            │  已对账    │
            │RECONCILED  │
            │    (3)     │
            └────────────┘

   任意状态（除已对账）可撤销
                  ↓
            ┌────────────┐
            │  已撤销    │
            │  REVOKED   │
            │    (2)     │
            └────────────┘

   对账发现差异
                  ↓
            ┌────────────┐
            │  差异      │
            │  DIFF      │
            │    (4)     │
            └────────────┘
```

### 3.4 与其他模块的协作

```
                 ┌──────────────────────────┐
                 │    医保对接 Insurance     │
                 └────────────┬─────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
   MedicareCatalog        MedicareProvider      MedicareSettle
   (医保目录)             (医保局接口)          (结算单)
        │                     │                     │
        ▼                     ▼                     ▼
   ES 检索              23省级平台适配          MySQL 分库分表
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                RocketMQ 事件 RxReviewedPassed / SettleCompleted
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
   Pay服务                DrugShip服务          Regulator服务
   (自付支付)             (触发配送)            (监管上报)
        │                     │                     │
        ▼                     ▼                     ▼
   对账系统                IM服务              诊疗事件
   (T+1 对账)             (消息推送)          (临床记录)
```

---

## 四、关键技术挑战与方案

### 4.1 23 省级医保平台接口差异统一

**挑战**：
- 国家医保局没有统一接口规范，各省自建平台
- 接口协议不同：HTTP / WebService / MQ
- 加密算法不同：SM2 / RSA / MD5
- 数据格式不同：JSON / XML
- 业务规则不同：起付线 / 封顶线 / 报销比例

**方案**：适配器模式 + 策略模式

```
接口架构：

InsuranceService（业务层）
       │
       ▼
MedicareProviderRouter（路由层）
   ├─ 按 province_code 路由
   └─ 选择对应的 Provider 实现
       │
       ▼
MedicareProvider接口（抽象层）
   ├─ queryEligibility()
   ├─ preSettle()
   ├─ realtimeSettle()
   ├─ revokeSettle()
   └─ querySettlement()
       │
       ▼
具体实现（23 个）
   ├─ BeijingMedicareProvider
   ├─ ShanghaiMedicareProvider
   ├─ GuangdongMedicareProvider
   └─ ...
```

**统一抽象**：

```java
public interface MedicareProvider {
    // 查询患者医保资质
    MedicareEligibility queryEligibility(String idCard, String provinceCode);
    
    // 预结算
    PreSettleResult preSettle(PreSettleRequest request);
    
    // 实时结算
    SettleResult realtimeSettle(SettleRequest request);
    
    // 撤销结算
    RevokeResult revokeSettle(String settleId, String reason);
    
    // 查询结算流水（对账用）
    List<SettleRecord> querySettlements(LocalDate date);
}

// 北京医保实现
@Component
public class BeijingMedicareProvider implements MedicareProvider {
    @Override
    public SettleResult realtimeSettle(SettleRequest request) {
        // 1. 构造北京医保局要求的 XML 报文
        String xml = buildBeijingXml(request);
        // 2. SM2 签名
        String signature = sm2Sign(xml, getBeijingPrivateKey());
        // 3. 调用北京医保局接口
        String response = httpClient.post(
            beijingConfig.getApiUrl(),
            xml,
            Map.of("X-Sign", signature));
        // 4. 解析返回
        return parseBeijingResponse(response);
    }
}
```

**新增省份接入流程**：
1. 实现 MedicareProvider 接口
2. 配置 MedicareProvider 表（接口地址、密钥等）
3. 同步该省医保目录
4. 测试环境联调（医保局提供测试号）
5. 灰度上线（先开通 1 家医院）

### 4.2 医保局接口稳定性与降级

**挑战**：
- 医保局接口稳定性参差不齐，部分省份可用性 < 99%
- 接口超时常见（3 秒以上）
- 维护窗口期（每月最后一个周末）
- 患者支付时如果医保局接口故障，影响业务

**方案**：多级降级 + 异步补偿

```
实时结算的降级策略：

Level 1（正常）：
   - 调用医保局接口，3 秒超时
   - 失败重试 2 次（间隔 500ms）
   - 成功率 > 99%

Level 2（医保局故障）：
   - 重试 2 次仍失败
   - 降级为"自费结算"（患者全自付，事后报销）
   - 提示患者："医保结算暂不可用，可选择自费支付，事后到医保局报销"
   - 记录"待补结算"状态，T+1 重试

Level 3（大面积故障）：
   - 单省份故障率 > 30%
   - 关闭该省份医保结算（MedicareProvider.enabled = 0）
   - 全部走自费结算
   - 监控告警 P1

Level 4（兜底）：
   - 患者可选择"暂不支付"
   - 处方保留 7 天有效期
   - 医保局恢复后再结算
```

**异步补偿**：

```
待补结算任务（每 30 分钟扫描）：
   SELECT * FROM medicare_settlement
   WHERE settle_status = 0  -- 预结算
     AND medicare_amount > 0  -- 有医保报销
     AND create_time < NOW() - INTERVAL 1 HOUR
     AND retry_count < 8

补偿流程：
1. 调用医保局接口补结算
2. 成功：更新结算单，通知患者"医保已补结算"
3. 失败：retry_count + 1，下次继续
4. 8 次仍失败：进入"待人工处理"
```

### 4.3 结算幂等与防重复扣款

**挑战**：
- 患者可能重复点击支付（网络抖动、APP 重启）
- 医保局接口超时但实际已扣款
- 必须保证同一笔处方只结算一次

**方案**：多层幂等

```
幂等三层防护：

Layer 1：前端
   - 支付按钮点击后立即禁用（防抖）
   - 客户端生成 clientRequestNo

Layer 2：业务层
   - medicare_settlement 表 uk_rx 唯一索引（一张处方只能有一笔结算）
   - medicare_req_no 唯一索引（同一请求号重复调用返回相同结果）

Layer 3：医保局接口
   - medicare_req_no 传给医保局
   - 医保局接口要求带 clientRequestNo，重复返回相同结果

幂等流程：
1. 患者点击支付
2. 生成 medicare_req_no = "MR" + rx_id + timestamp
3. 查询 medicare_settlement 表
   ├─ 已存在且 status=1（已结算）：直接返回结算结果
   └─ 不存在或 status=0：调用医保局
4. 调用医保局接口（带 medicare_req_no）
   ├─ 成功：更新结算单
   ├─ 超时：查询医保局流水
   │   ├─ 已结算：补偿更新本地结算单
   │   └─ 未结算：重试
   └─ 失败：返回错误，患者重试
```

### 4.4 对账系统与差错处理

**挑战**：
- 每日 4.2w 笔结算，对账必须 100% 准确
- 平台与医保局记录可能存在金额差异、记录缺失、记录多余
- 差异必须 24 小时内处理

**方案**：四阶段对账

```
阶段 1：数据准备（T+1 02:00）
   - 平台侧：导出当日所有 settle_status=1 的结算单
   - 医保侧：调用医保局接口拉取当日结算流水
   - 双方数据写入 reconcile_record 表（diff_type=0）

阶段 2：逐笔比对（T+1 03:00）
   - 按 settle_id 匹配双方记录
   - 匹配成功：比对金额
     ├─ 金额一致：diff_type=0（无差异）
     ├─ 平台 > 医保局：diff_type=1（短款，平台少收）
     └─ 平台 < 医保局：diff_type=2（长款，平台多收）
   - 匹配失败：
     ├─ 平台有，医保局无：diff_type=3（缺失）
     └─ 医保局有，平台无：diff_type=4（多余）

阶段 3：差异处理（T+1 04:00）
   - 短款（平台少收）：
     ├─ 差异 < 1 元：挂账，月终汇总处理
     └─ 差异 >= 1 元：调用医保局补扣款接口
   - 长款（平台多收）：
     ├─ 差异 < 1 元：挂账
     └─ 差异 >= 1 元：调用医保局退款接口
   - 缺失：重新调用医保局结算接口
   - 多余：标记为"医保局多结算"，联系医保局处理

阶段 4：报告与监控（T+1 06:00）
   - 生成对账报告
   - 差异率 = 差异笔数 / 总笔数
   - 差异率 > 0.1% 触发 P1 告警
   - 差异金额 > 1000 元触发 P0 告警
```

**对账算法**：

```java
public void reconcile(LocalDate date) {
    // 1. 拉取双方数据
    List<SettleRecord> localRecords = settleRepository.findByDate(date);
    List<SettleRecord> medicareRecords = medicareProvider.querySettlements(date);
    
    // 2. 构建Map（key=settle_id）
    Map<String, SettleRecord> localMap = localRecords.stream()
        .collect(toMap(SettleRecord::getSettleId, r -> r));
    Map<String, SettleRecord> medicareMap = medicareRecords.stream()
        .collect(toMap(SettleRecord::getSettleId, r -> r));
    
    // 3. 逐笔比对
    Set<String> allKeys = new HashSet<>();
    allKeys.addAll(localMap.keySet());
    allKeys.addAll(medicareMap.keySet());
    
    for (String settleId : allKeys) {
        SettleRecord local = localMap.get(settleId);
        SettleRecord medicare = medicareMap.get(settleId);
        
        ReconcileRecord record = new ReconcileRecord();
        record.setSettleId(settleId);
        
        if (local == null) {
            // 平台无，医保局有
            record.setDiffType(4);  // 多余
            record.setDiffAmount(medicare.getMedicareAmount());
        } else if (medicare == null) {
            // 平台有，医保局无
            record.setDiffType(3);  // 缺失
            record.setDiffAmount(local.getMedicareAmount());
        } else {
            // 双方都有，比对金额
            BigDecimal diff = local.getMedicareAmount()
                .subtract(medicare.getMedicareAmount());
            if (diff.compareTo(BigDecimal.ZERO) == 0) {
                record.setDiffType(0);  // 无差异
            } else if (diff.compareTo(BigDecimal.ZERO) > 0) {
                record.setDiffType(1);  // 短款
            } else {
                record.setDiffType(2);  // 长款
            }
            record.setDiffAmount(diff.abs());
        }
        
        reconcileRepository.save(record);
    }
}
```

### 4.5 医保目录同步与更新

**挑战**：
- 国家医保目录每年调整 1-2 次，省级目录可能每月调整
- 目录数据量大（12.6w 条），全量同步耗时长
- 增量同步需要识别变更
- 同步期间不能影响业务查询

```
同步策略：

全量同步（每月 1 次）：
1. 调用医保局接口拉取全量目录（分页，每页 1000 条）
2. 写入新表 medicare_catalog_new
3. 全量校验（条数、关键字段）
4. 原子切换：RENAME TABLE medicare_catalog TO medicare_catalog_old,
              medicare_catalog_new TO medicare_catalog
5. 同步到 ES
6. 保留老表 7 天，便于回滚

增量同步（每日 02:00）：
1. 调用医保局接口拉取当日变更（增量接口）
2. 直接 UPDATE medicare_catalog
3. 增量同步到 ES
4. 失败重试 3 次，仍失败告警

ES 同步：
- 用 Canal 监听 MySQL binlog，实时同步到 ES
- ES 索引：drug_code + province_code 联合索引
- 查询：按药品编码 + 省份查询医保信息，< 10ms

24 小时内同步保障：
- 全量同步在低峰期（02:00-05:00）执行
- 同步失败有重试机制
- 监控告警：同步延迟 > 24 小时触发 P1
```

### 4.6 跨地区医保结算

**挑战**：
- 患者可能在 A 省参保，但在 B 省就诊（异地就医）
- 异地就医需要备案
- 不同省份报销政策不同

**方案**：

```
异地就医流程：
1. 患者先在参保地（A 省）备案
2. 备案信息上传到国家医保平台
3. 患者在就诊地（B 省）医院就诊
4. 平台调用国家医保平台查询备案信息
5. 调用 B 省医保目录计算报销
6. 实时结算时调用 B 省医保局接口
7. 资金由 B 省医保局垫付，A 省医保局事后清算

实现：
- MedicareEligibility 增加"参保地"和"就诊地"字段
- 异地就医时优先用就诊地医保目录
- 结算时调用就诊地医保局接口
- 对账时区分本地结算和异地结算
```

---

## 五、容量规划与性能指标

### 5.1 容量规划

```
日均结算量：4.2w 笔
峰值结算量：7.8w 笔/日
峰值 QPS：50（实时结算集中在 9:00-11:00）

资源估算：
- 单结算单数据量：~8KB（含 detail_items JSON）
- 日均数据增长：4.2w × 8KB = 336MB/日
- 年数据增长：~120GB/年
- 5 年存储：~600GB（MySQL）+ ES ~200GB

MySQL 分库分表：
- 按 patient_id 分片（与问诊订单一致）
- 64 库 × 16 表 = 1024 表
- 单表 5 年数据量：7.8w × 365 × 5 / 1024 ≈ 1400w 条
- 单表 1400w 条，索引优化后查询 < 100ms
```

### 5.2 性能指标

| 接口 | QPS | P99 延迟 | 优化点 |
|------|-----|---------|--------|
| 医保预结算 | 200 | < 500ms | ES 查询 + 本地计算 |
| 医保实时结算 | 50 | < 2s | 医保局接口（外部） |
| 医保目录查询 | 1000 | < 50ms | ES 缓存 |
| 结算单查询 | 500 | < 100ms | Redis 缓存 |
| 对账任务 | - | 4 小时完成 | 并行处理 + 批量 |

### 5.3 监控指标

```
业务指标：
- 结算成功率（目标 > 99.5%）
- 预结算延迟 P99（目标 < 500ms）
- 实时结算延迟 P99（目标 < 2s）
- 对账差异率（目标 < 0.1%）
- 对账完成时间（目标 < 4 小时）

技术指标：
- 医保局接口调用延迟 P99（目标 < 2s）
- 医保局接口失败率（目标 < 1%）
- ES 查询延迟 P99（目标 < 50ms）
- MySQL 慢查询数（目标 < 10/分钟）

合规指标：
- 目录同步延迟（目标 < 24 小时）
- 对账完成率（目标 100%）
- 差异处理及时率（目标 100% 24h 内）
```

---

## 六、可观测性与运维

### 6.1 日志规范

```
关键字段：
- traceId：链路追踪ID
- settleId：结算单号
- rxId：处方号
- patientIdHash：患者ID哈希（隐私保护）
- provinceCode：省份编码
- medicareReqNo：医保局请求号
- eventType：PRE_SETTLE/REALTIME_SETTLE/REVOKE/RECONCILE
- duration：接口耗时
- medicareRespNo：医保局返回流水号

日志示例：
2025-12-01 10:30:00.123 INFO  InsuranceService - traceId=xxx settleId=MS2025... 
  rxId=RX2025... provinceCode=11 eventType=REALTIME_SETTLE 
  duration=1873ms medicareReqNo=MR... medicareRespNo=MR-RESP-...
```

### 6.2 链路追踪

```
关键链路：
处方审核通过 -> 医保预结算 -> 患者支付 -> 医保实时结算 -> 药品配送 -> T+1 对账

SkyWalking 追踪点：
- InsuranceService.preSettle
- MedicareProviderRouter.route
- BeijingMedicareProvider.realtimeSettle
- PayService.pay
- ReconcileJob.reconcile
```

### 6.3 告警规则

```
P0 告警（立即处理，电话通知）：
- 对账差异金额 > 1000 元
- 医保局接口大面积故障（> 30% 失败率）
- 对账任务未在 06:00 前完成

P1 告警（立即处理）：
- 结算成功率 < 99%
- 实时结算延迟 P99 > 3s
- 对账差异率 > 0.1%
- 目录同步延迟 > 24 小时

P2 告警（1 小时内处理）：
- 单省份医保局接口失败率 > 5%
- 预结算延迟 P99 > 1s
- 待补结算任务堆积 > 100
```

---

## 七、面试追问点

### Q1：医保对接最难的是什么？

**核心答案**：23 个省级医保平台的接口差异统一。
- 接口协议不同（HTTP / WebService / MQ）
- 加密算法不同（SM2 / RSA / MD5）
- 数据格式不同（JSON / XML）
- 业务规则不同（起付线 / 封顶线 / 报销比例）
- 解决方案：适配器模式 + 策略模式，定义 MedicareProvider 接口，每个省份一个实现

### Q2：医保局接口故障怎么办？业务怎么降级？

**核心答案**：多级降级 + 异步补偿
- Level 1（正常）：3 秒超时，重试 2 次
- Level 2（医保局故障）：降级为"自费结算"，事后报销
- Level 3（大面积故障）：关闭该省份医保结算，全部走自费
- Level 4（兜底）：患者可选择"暂不支付"，处方保留 7 天有效期
- 异步补偿：每 30 分钟扫描"待补结算"任务，重试 8 次

### Q3：对账系统怎么设计的？差异怎么处理？

**核心答案**：四阶段对账
- 阶段 1：数据准备（T+1 02:00），拉取双方数据
- 阶段 2：逐笔比对（T+1 03:00），按 settle_id 匹配，识别差异类型
- 阶段 3：差异处理（T+1 04:00），短款补扣 / 长款退款 / 缺失补结算 / 多余联系医保局
- 阶段 4：报告与监控（T+1 06:00），差异率 > 0.1% 触发 P1
- 1 元以下差异挂账月终汇总，1 元以上立即处理

### Q4：医保结算怎么保证幂等？防止重复扣款？

**核心答案**：三层幂等防护
- Layer 1 前端：按钮防抖，生成 clientRequestNo
- Layer 2 业务层：medicare_settlement 表 uk_rx 唯一索引（一张处方只能有一笔结算），medicare_req_no 唯一索引
- Layer 3 医保局接口：medicare_req_no 传给医保局，重复返回相同结果
- 超时处理：查询医保局流水，已结算则补偿更新本地，未结算则重试

### Q5：医保目录怎么同步？12.6w 条数据怎么处理？

**核心答案**：全量 + 增量双策略
- 全量同步（每月 1 次）：拉取全量目录，写入新表，原子切换（RENAME TABLE）
- 增量同步（每日 02:00）：调用医保局增量接口，直接 UPDATE
- ES 同步：Canal 监听 MySQL binlog，实时同步到 ES
- 查询走 ES，< 10ms
- 24 小时内同步保障：失败重试 3 次，告警监控

### Q6：异地就医怎么处理？

**核心答案**：备案 + 就诊地结算
- 患者先在参保地备案，备案信息上传到国家医保平台
- 平台调用国家医保平台查询备案信息
- 调用就诊地医保目录计算报销
- 实时结算时调用就诊地医保局接口
- 资金由就诊地医保局垫付，参保地医保局事后清算
- 对账时区分本地结算和异地结算

### Q7：医保结算和处方开具、支付的关系是什么？

**核心答案**：
- 处方开具（Rx 模块）产出处方，触发医保预结算
- 医保预结算计算医保报销金额 + 患者自付金额
- 患者点击支付时，调用医保实时结算接口（确认报销）
- 实时结算成功后，患者支付自付部分（Pay 服务）
- 支付成功后触发药品配送
- T+1 对账确保平台与医保局记录一致

### Q8：医保数据涉及患者隐私，怎么保护？

**核心答案**：
- 医保卡号 AES-256 加密存储，密钥由 KMS 托管
- 身份证号用 SHA256 哈希存储（patient_id_hash），用于查询
- 日志不打印明文医保卡号、身份证号
- 数据库审计日志，记录所有查询操作
- 等保三级要求：数据传输 mTLS，存储加密，访问审计

### Q9：医保结算的 SLA 怎么保障？

**核心答案**：
- 实时结算延迟 < 2 秒：医保局接口 3 秒超时，重试 2 次
- 结算成功率 99.92%：医保局接口故障降级为自费结算
- 对账差异率 < 0.05%：四阶段对账 + 自动差异处理
- 对账完成时间 T+1 06:00：并行处理 + 批量优化
- 监控告警：P0/P1/P2 三级告警，差异金额 > 1000 元立即电话通知

### Q10：如果让你重新设计这个模块，你会做什么不同？

**核心答案**：
1. **引入医保结算引擎**：用规则引擎（Drools）配置报销规则，减少代码硬编码
2. **支持更多医保场景**：门诊统筹、门诊慢病、住院结算（当前只支持门诊个人账户）
3. **医保智能问答**：基于医保目录 + LLM，回答患者"这个药能报多少"
4. **异地就医全流程**：当前需要患者先备案，未来对接国家医保平台自动备案
5. **医保欺诈检测**：基于历史数据 + ML，识别异常结算（如频繁开药、超量开药）

---

## 八、附录：与往周专题的呼应

### 8.1 与支付系统专题的呼应（6月第5周）

| 维度 | 支付系统 | 医保对接 |
|------|---------|---------|
| 幂等设计 | 支付流水号 | medicare_req_no |
| 对账机制 | T+1 对账 | T+1 对账（四阶段） |
| 差错处理 | 短款补扣 / 长款退款 | 同左 |
| 退款流程 | 退款单 + 状态机 | 撤销结算 + 状态机 |
| 渠道路由 | 多支付渠道 | 多省份医保平台 |

**核心差异**：支付系统对接第三方支付（支付宝/微信），医保对接对接政府医保局。政府接口稳定性差，降级策略更重要。

### 8.2 与医疗信息化专题的呼应（7月第1周、第2周）

- **医保结算底层原理**：7月第1周 Day07 深挖过"医保结算底层原理与四防闭环"，本模块是其工程实现
- **HL7/FHIR**：医保接口部分用 HL7 V3 报文（少数省份）
- **EMR 集成**：医保结算单需同步到执业挂靠医院 EMR
- **医保结算四防闭环**：防漏报 / 防错报 / 防重报 / 防延迟，本模块实现了四防

### 8.3 与分布式事务专题的呼应（5月第3周、第4周）

- **TCC 分布式事务**：医保结算 + 患者支付 + 药品配送是跨服务事务，用 TCC 保证最终一致
- **事务消息**：处方审核通过事件 -> 医保预结算，用 RocketMQ 事务消息
- **幂等消费**：下游服务消费事件需幂等（基于 medicare_req_no）

---

## 九、附录：技术栈与依赖

| 技术 | 用途 | 选型理由 |
|------|------|---------|
| Spring Cloud Alibaba | 微服务框架 | 公司技术栈统一 |
| RocketMQ 5.x | 事务消息 | 处方事件与医保结算一致性 |
| MySQL 8.0 + ShardingSphere | 分库分表 | 按 patient_id 分片 |
| Redis 7.x Cluster | 缓存 + 幂等 | 结算单缓存 |
| Elasticsearch 8.x | 医保目录检索 | 12.6w 条目录快速查询 |
| Canal | MySQL -> ES 同步 | 实时同步目录变更 |
| HSM 加密机 | 密钥保护 | 医保接口签名 |
| 国密 SM2 / SM4 | 加密算法 | 医保合规要求 |
| SkyWalking | 链路追踪 | 跨服务追踪 |
| XXL-Job | 对账任务调度 | T+1 定时对账 |
| Prometheus + Grafana | 监控告警 | 业务 + 技术指标 |
