# 架构师学习-能力差距梳理（2026年08月第1周）

> 周主题：JVM 专题第 2 周 - 调优实战与生产排查
>
> 按主题组织，每个差距标注发现日期。跨周延续的差距标注来源。
>
> 累计差距延续：截至第5周末累计 117+ 项，本周新增 50 项（Day01 7 + Day02 10 + Day03 8 + Day04 9 + Day05 8 + Day06 8），累计 167+ 项。
>
> 本周覆盖：Day01 JVM 调优参数全解 / Day02 JVM 诊断工具链实战 / Day03 OOM 排查实战 / Day04 CPU 飙高排查实战 / Day05 在线问诊系统 JVM 实战 / Day06 串联整合-故障复盘方法论

---

## 一、JVM 调优参数全解（Day01）

### 差距1.1：参数与版本演进的对应关系不熟
> Day1发现，延续第5周差距3.4 / 4.5 / 6.3

- **现状**：知道 JDK 8 用 `-XX:+PrintGCDetails`、JDK 11+ 用 `-Xlog:gc*`，但 CMS 各版本演进（JDK 9 Deprecated / JDK 14 移除）、BiasedLocking 在 JDK 15 废弃（JEP 374）、ZGC 在 JDK 15 转正等版本边界细节不熟
- **架构师水平**：能脱口而出 JDK 8 / 11 / 17 / 21 各版本参数差异表；能在 JDK 升级评审时 30 分钟内列出所有需要迁移的参数；能用 `jcmd PID VM.flags` 验证参数实际生效情况
- **补足方向**：精读 JEP 291（CMS Deprecated）/ JEP 363（CMS 移除）/ JEP 374（Biased Locking Deprecated）/ JEP 377（ZGC Production）/ JEP 439（分代 ZGC）；在测试环境跑一次 JDK 8 -> 17 参数迁移演练

### 差距1.2：MaxGCPauseMillis 的"目标 vs 上限"陷阱不深
> Day1发现

- **现状**：知道 `-XX:MaxGCPauseMillis=50` 是"目标"，但实际生产中"G1 没达到目标该怎么调"的工程经验不足，经常误以为"设小就有效"
- **架构师水平**：能讲清 MaxGCPauseMillis 与 G1NewSizePercent / G1MaxNewSizePercent / IHOP / G1ReservePercent 的耦合关系；能在 30 分钟内画出"调参耦合矩阵"；能根据 GC 日志反推哪个参数需要调整
- **补足方向**：第 2 周 Day05 实测 G1 调优时建立"调参矩阵"；调研 Netflix / LinkedIn 的 G1 调优案例；阅读 R大博客关于 G1 自适应调优的 3 篇文章

### 差距1.3：指针压缩 32GB 边界的实战经验不足
> Day1发现

- **现状**：知道 32GB 是指针压缩边界，但"31GB 堆 vs 33GB 堆"的实测性能差异、为什么"32GB 堆 + 单实例"不如"30GB 堆 + 多实例"、JDK 15+ 的指针压缩新特性（HotSpot 仍维持 32GB）不深
- **架构师水平**：能用 JMH 实测 24G / 30G / 32G / 36G 堆的吞吐与 GC 停顿曲线；能在容量规划阶段决定"大堆单实例 vs 小堆多实例"
- **补足方向**：用 IM 网关做一次 24G vs 32G 实测；调研 Azul Zing / Prime 的"无指针压缩限制"方案；阅读 OpenJDK `compressedOops.cpp` 源码

### 差距1.4：容器化 JVM 参数的实战坑不熟
> Day1发现，延续第3周（7月第3周 K8s）差距3.7 / 6.2

- **现状**：知道 `-XX:+UseContainerSupport` 默认开启，但 cgroup v2 兼容性、CPU Throttling 对 JVM Safepoint 的影响、`-XX:ParallelGCThreads` 在容器中为什么要手动设的工程经验不足
- **架构师水平**：能讲清 JDK 8u191 之前的两个经典容器问题（内存不看 limit / CPU 不看 limit）；能诊断 CPU Throttling 引发的偶发 P99 飙高；能根据 K8s `requests/limits` 精确推算 JVM 参数（`-XX:InitialRAMPercentage` / `-XX:MaxRAMPercentage`）
- **补足方向**：在 K8s 集群实测 cgroup v2 下 JVM 行为；调研 Google Cadvisor / kubelet cgroup 监控；阅读 JEP 343（Packaging Tool）与容器化演进

### 差距1.5：JIT 参数与 GC / Safepoint 的协同不深
> Day1发现，延续第5周差距5.7

- **现状**：知道 `-XX:+TieredCompilation` / `-XX:CompileThreshold`，但分层编译开启后实际触发 C1/C2 的阈值（Tier 3 约 200 次 / Tier 4 约 10000-15000 次）、JIT 编译线程占 CPU 与 GC Safepoint 的交互不深
- **架构师水平**：能用 `-XX:+PrintCompilation` 跟踪 100 个方法的编译历史；能诊断"JIT 编译线程占 30% CPU"的异常；能设计预热方案避免冷启动 JIT 抖动
- **补足方向**：阅读 OpenJDK `compileBroker.cpp` / `simpleThresholdPolicy.cpp`；调研 AppCDS / AOT 编译在 JDK 17 的实战；用 JMH 测试预热前后的性能差异

### 差距1.6：-XX 陷阱与生产配置模板的实战经验不足
> Day1发现

- **现状**：知道开关型（`+`/`-`）与赋值型（`=`）区别，但 `-XX:+DisableExplicitGC` 的副作用（Direct ByteBuffer 清理失效）、`-XX:+UseStringDeduplication` 的适用场景、`-XX:+HeapDumpOnOutOfMemoryError` 的磁盘预留计算等工程经验不足
- **架构师水平**：能为不同业务场景（IM / 视频 / 监管 / 历史归档）定制生产配置模板；能 30 分钟内排查"配置了 OOM 自动 dump 但没生效"的问题（路径权限 / 磁盘满 / 容器 rootfs 不可写）
- **补足方向**：整理团队 JVM 生产配置模板库；调研阿里 / 美团 / 字节跳动的 JVM 参数规范；在测试环境复现"DisableExplicitGC 导致 Direct ByteBuffer OOM"

### 差距1.7：JVM 内存规划的全局视角不足
> Day1发现，延续第4周简历项目差距1.5

- **现状**：能估算堆 / 直接内存 / 元空间的基础占用，但 Code Cache / Thread Stack / GC 日志 / Heap Dump 预留 / JVM 自身开销（约 200-500MB）的全局视角不足，导致 K8s limit 设得偏小被 OOM kill
- **架构师水平**：能为 4C8G / 8C16G / 16C32G Pod 设计标准 JVM 内存配比模板；能用 NMT（Native Memory Tracking）精确测算 JVM 自身开销
- **补足方向**：用 `jcmd PID VM.native_memory summary` 实测 IM 网关的 JVM 内存构成；调研 Netflix 容量规划方法论；为在线问诊系统设计 3 套标准 Pod 配比

---

## 二、JVM 诊断工具链实战（Day02）

### 差距2.1：JDK 自带命令行工具的版本差异不熟
> Day2发现

- **现状**：知道 jps / jstat / jmap / jstack 基础用法，但 JDK 8 vs JDK 11+ 的工具差异（jmap 大堆卡死 vs jcmd 稳定 / jhat 废弃 / JFR 开源）不熟；`jstack -F` / `-l` / `-m` 各选项的适用场景容易混淆
- **架构师水平**：能不查文档直接写出 `jcmd PID GC.heap_dump /tmp/x.bin` / `jstack -l PID` / `jstat -gcutil PID 1000 10`；能讲清为什么 JDK 11+ 推荐 jcmd 而非 jmap；能用 JFR 替代传统 jstack/jmap 做持续低开销采样
- **补足方向**：每日练 1 个工具（周一 jstack、周二 jmap、周三 jcmd、周四 JFR、周五 jstat）；阅读 OpenJDK `serviceability` 模块源码；在测试环境复现"jmap 在 32GB 堆上卡死"

### 差距2.2：Arthas 命令的熟练度不足
> Day2发现，延续第4周简历项目差距

- **现状**：知道 Arthas dashboard / thread / jad / watch / trace 基础用法，但 vmtool / ognl / redefine / profiler 等高阶命令不熟；`watch` 表达式语法（`params` / `returnObj` / `target` / `#cost`）容易写错；`trace -n N` 限制次数的工程实践不足
- **架构师水平**：能用 `vmtool --action getInstances --className com.github.benmanes.caffeine.cache.BoundedLocalCache` 直接查缓存实例；能用 `ognl '@com.example.Config@DEBUG = true'` 不重启改日志级别；能用 `redefine` 热更新字节码（含限制：不能改方法签名 / 字段 / 继承关系）
- **补足方向**：精读 Arthas 官方文档高级用法；每周练 2 个高阶命令；调研 Arthas 在 K8s sidecar 模式的部署最佳实践

### 差距2.3：MAT 堆内存分析的深度不足
> Day2发现

- **现状**：知道 Histogram / Dominator Tree / Leak Suspects 三大视图，但 Shallow Size vs Retained Size 的本质区别、`Path to GC Roots` vs `Merge Shortest Paths` 的适用场景、OQL 查询语法、10GB+ 大 dump 的处理方法不深
- **架构师水平**：能用 OQL `SELECT s.size FROM java.util.HashMap s WHERE s.@retainedHeapSize > 1000000` 查大 Map；能配置 MAT `-Xmx20g` 处理 10GB dump；能 1 分钟内从 Dominator Tree 找到 Retained Top 1 元凶
- **补足方向**：精读 Eclipse MAT 官方文档；本地装 MAT 实战 3 个真实 dump；调研在线 MAT（heapdump.io）的隐私合规问题

### 差距2.4：jstack + 火焰图实战经验不足
> Day2发现，延续第5周差距7.3

- **现状**：知道 `top -Hp` + `printf %x` + jstack 的 5 步法，但火焰图解读能力不足（"宽且尖"vs"宽且平"的判断）、async-profiler 的 4 种模式（cpu / alloc / lock / wall）的适用场景、JFR 持续录制的生产配置不熟
- **架构师水平**：能在 5 分钟内从火焰图找出 CPU 占 80% 的单一热点方法；能用 `async-profiler -e alloc` 找出内存分配压力大的方法；能为生产服务配置 JFR 持续录制（maxage=1h / maxsize=200M）
- **补足方向**：本地装 async-profiler 实战 5 个火焰图；调研 Netflix FlaPoodle 火焰图产品；阅读 Brendan Gregg 火焰图论文

### 差距2.5：GC 日志分析的实战经验不足
> Day2发现，延续第5周差距2.5

- **现状**：知道 JDK 8 用 `-XX:+PrintGCDetails`、JDK 11+ 用 `-Xlog:gc*`，但 G1 Mixed GC 日志中 `[Pause (G1 Evacuation Pause) (young) (initial-mark) (mixed)` 各字段含义、`to-space exhausted` 的诊断、gceasy.io 在线分析的关键指标（Throughput / Avg Pause / Max Pause）的实战经验不足
- **架构师水平**：能 30 秒内从 GC 日志判断"Minor GC 频繁 / Mixed GC 慢 / Full GC 失败"三类问题；能用 GCViewer 离线分析 1 周 GC 日志找趋势；能为生产服务设计 GC 监控告警（堆 > 85% / Full GC > 1次/分钟）
- **补足方向**：收集 5 份真实生产 GC 日志做分析练习；用 Filebeat + ES + Kibana 搭建 GC 日志监控；调研 Datadog / Dynatrace 的 GC 监控产品

### 差距2.6：5 分钟定位 Full GC 元凶的节奏感不足
> Day2发现，延续第5周差距7.4

- **现状**：知道"5 分钟定位"的思路，但实际生产中"分秒级"动作清单（0:00-0:30 现象确认 / 0:30-1:00 止损决策 / 1:00-2:00 抓现场 / 2:00-3:00 日志分析 / 3:00-4:00 堆分析 / 4:00-5:00 根因止血）的节奏感不足；"摘流量 + 抓 dump"的执行顺序容易出错（先抓 dump 会 STW 引发雪崩）
- **架构师水平**：能背出 5 分钟定位法的 6 个节奏点；能在 30 秒内做止损决策（摘流量 / 限流 / 降级 / 重启）；能 1 分钟内抓完 jstack + jmap dump + GC 日志三件套
- **补足方向**：每月一次故障演练（注入内存泄漏）；整理团队"5 分钟定位 SOP"；调研 Google SRE 故障排查手册

### 差距2.7：工具链副作用的工程意识不足
> Day2发现

- **现状**：知道 jmap / jstack / Arthas 有副作用，但 `jmap -histo:live` 触发 Full GC、`jstack -F` 挂起进程、`Arthas trace` 高 QPS 长挂导致 RT 飙高、`Arthas redefine` 反复试错导致 Metaspace 膨胀等副作用的实战意识不足
- **架构师水平**：能为每个工具建立"副作用清单"与"何时禁用"判断标准；能在生产事故中快速决策"用哪个工具副作用最小"；能用 `monitor` 替代 `watch` 降低开销
- **补足方向**：整理团队"工具链副作用速查表"；在测试环境实测 Arthas trace 在 1000 QPS 下的 RT 影响；调研阿里 / 美团的工具链使用规范

### 差距2.8：工具链体系建设不足
> Day2发现，延续第5周差距6.5 / 7.3

- **现状**：会使用单个工具，但"工具链体系建设"（JFR 长开 / Arthas sidecar / dump 路径预留 / 监控告警 / 故障知识库 / 定期演练）的架构师视角不足
- **架构师水平**：能为团队设计完整的 JVM 工具链体系（持续录制 + 自动告警 + 故障自愈）；能搭 Arthas tunnel server 集中管理多 Pod；能设计混沌工程演练机制
- **补足方向**：调研 Datadog APM / Dynatrace 的 JVM 监控产品；搭建 IM 网关的工具链体系 PoC；调研 Netflix Chaos Monkey 故障注入实践

### 差距2.9：K8s 容器内工具链使用经验不足
> Day2发现，延续第3周（7月第3周 K8s）差距

- **现状**：知道 `kubectl exec` 进 Pod 用 Arthas，但 K8s 1.12+ 共享 PID namespace 的 sidecar 模式、Arthas tunnel server 远程连接、init container 注入 Arthas 等 K8s 原生方式不熟
- **架构师水平**：能为团队设计 K8s 上的 Arthas 标准接入方式（sidecar + tunnel）；能解决"Pod 重启 Arthas 丢失"问题；能用 kubectl debug 一键进入诊断容器
- **补足方向**：在测试 K8s 集群搭建 Arthas sidecar PoC；调研阿里 ACK / 字节 TKE 的 JVM 诊断产品；阅读 K8s 1.25+ ephemeral container 文档

### 差距2.10：在线问诊系统工具链实战案例不足
> Day2发现，延续第4周简历项目差距

- **现状**：能讲工具用法，但与简历项目（在线问诊系统）结合的实战案例（IM 网关 10w 长连接 CPU 飙高 / 视频 RTP 包堆积 Full GC / 监管上报 OOM / 问诊订单缓存膨胀 / MongoDB 大文档 Humongous）的完整故事不熟
- **架构师水平**：能讲出 5 个真实排查案例（现象 -> 工具 -> 根因 -> 修复）；能在面试中用 STAR 法则结构化讲述；能用案例反推架构改进点
- **补足方向**：第 2 周 Day05 实战日产出 5 个案例文档；整理团队"故障案例库"；用 STAR 法则演练 5 次面试讲述

---

## 三、OOM 排查实战（Day03）

### 差距3.1：OOM 类型识别与版本演进对应关系不熟
> Day3发现，延续第5周差距3.4 / 7.4

- **现状**：能识别 `Java heap space` 与 `Metaspace`，但 `GC overhead limit exceeded` 本质是"堆 OOM 预警"（98% 时间 GC 仍回收不下来）、`Metaspace` OOM 与 `Compressed class space` OOM 的关系（前者是元数据区，后者是指针压缩下的类空间子区）、`StackOverflowError` 在 JVM 规范层面不算 OOM 但工程上常一起讨论等细节不深
- **架构师水平**：能 30 秒内从报错信息识别 8 种 OOM 类型并脱口而出修复方向；能讲清 JDK 8 PermGen -> JDK 8+ Metaspace 演进对 OOM 类型的影响；能讲清 `-XX:MaxMetaspaceSize` / `-XX:CompressedClassSpaceSize` / `-XX:MaxDirectMemorySize` 各自防御哪类 OOM
- **补足方向**：精读 JVM 规范第 2.5.4 节（Run-Time Data Areas）；调研 JEP 383（Metaspace 重新设计）；在测试环境复现 8 种 OOM 各一次

### 差距3.2：Heap Dump 方式选择与时机判断不精
> Day3发现，延续第5周差距7.4

- **现状**：知道 `jmap -dump` 和 `-XX:+HeapDumpOnOutOfMemoryError`，但 5 种 dump 方式（自动 / jcmd / jmap / Arthas / kill -3 + jhsdb）的差异、STW 时长、是否包含 unreachable 对象、是否触发 GC 的选择决策不精；"自动 dump 未必抓得到元凶"（OOM 前已经 Full GC 多次，对象状态已变）的工程意识不足
- **架构师水平**：能不查文档选对 dump 方式（生产用自动 + 主动 jcmd 双保险）；能讲清 `jmap -dump:live` 与 `jmap -dump` 的区别（前者触发 Full GC 只 dump 存活对象）；能讲清为什么 OOM 时建议"先摘流量 + 主动 dump"而非等自动 dump
- **补足方向**：在测试环境实测 5 种 dump 方式的 STW 时长与文件大小差异；调研 Alibaba Arthas heapdump 与 jcmd 的实现差异；阅读 OpenJDK `heapDumper.cpp` 源码

### 差距3.3：MAT 支配树算法与 OQL 深度不足
> Day3发现，延续第5周差距7.5

- **现状**：会用 MAT 看 Histogram / Dominator Tree / Leak Suspects，但支配树 Dominator Tree 的算法原理（IBM paper：Largest Retained Set 算法）讲不清；Shallow Size vs Retained Size 的本质区别（支配集）不深；OQL 语法（`SELECT s FROM java.util.HashMap s WHERE s.@retainedHeapSize > 1000000`）写不熟；10GB+ 大 dump 处理（MAT `-Xmx20g`、`-XX:+HeapDumpOnOutOfMemoryError` 离线机器、在线 MAT、`--loader` 模式）的工程经验不足
- **架构师水平**：能讲清支配树的"X 支配 Y 当且仅当所有从 GC Roots 到 Y 的路径都经过 X"定义；能 1 分钟内从 Dominator Tree 找到 Retained Top 1 元凶；能用 OQL 查询特定模式的对象（如所有 size > 10000 的 HashMap）；能为 10GB dump 准备 20GB 内存的离线分析机
- **补足方向**：精读 Eclipse MAT 官方文档 + IBM Dominator Tree paper；本地装 MAT 实战 3 个真实 dump；调研 heapdump.io 在线 MAT 的隐私合规问题

### 差距3.4：5 类内存泄漏模式的识别清单不熟
> Day3发现

- **现状**：知道缓存膨胀和 ThreadLocal，但 5 类模式（缓存膨胀 / ThreadLocal 累积 / 监听器 Callback 未 unregister / 静态集合无限增长 / finalize 队列堆积）的典型代码、识别清单、修复方案不熟；"ThreadLocal 在线程池下是隐形杀手"（线程池复用导致 ThreadLocalMap 累积）的原理讲不清；finalize 为什么被 JDK 9 标记 Deprecated、JDK 18 提案移除的原因（性能差 + 易内存泄漏 + 替代方案 Cleaner）不深
- **架构师水平**：能背出 5 类泄漏模式的识别清单（如 Caffeine 是否设了 maximumSize / ThreadLocal 是否在 finally 中 remove / 监听器是否在不需要时 unregister）；能从 Code Review 中识别"线程池 + ThreadLocal"反模式；能讲清 Cleaner（JDK 9+）替代 finalize 的原理
- **补足方向**：整理团队"5 类泄漏模式识别 Checklist"；调研阿里 P3C 静态扫描规则；在测试环境复现 ThreadLocal 在线程池下的累积

### 差距3.5：5 分钟定位 OOM 元凶的节奏感不足
> Day3发现，延续第5周差距7.4 / Day2差距2.6

- **现状**：知道"5 分钟定位"思路，但 OOM 场景下的 6 个节奏点（0:00-0:30 现象确认 / 0:30-1:00 止损决策 / 1:00-2:00 抓现场 / 2:00-3:00 GC 日志分类 / 3:00-4:00 MAT 分析 / 4:00-5:00 根因止血）的关键决策不精；"OOM 自动重启 vs 摘流量抓现场"的权衡（核心业务中断 vs 现场丢失）容易选错；GC 日志分类（频繁 Minor / Mixed 慢 / Full 失败）的判断标准不熟
- **架构师水平**：能背出 6 个节奏点的关键动作；能在 30 秒内做止损决策（P0 立即重启 / P1 摘流量抓现场）；能 1 分钟内抓完 dump + GC 日志 + jstack 三件套；能从 GC 日志 30 秒分类元凶类型
- **补足路径**：每月一次 OOM 故障演练（注入内存泄漏）；整理团队"OOM 5 分钟定位 SOP"；用 STAR 法则整理 5 个 OOM 真实案例

### 差距3.6：大 dump 处理与离线分析工程化不足
> Day3发现

- **现状**：本地 MAT 分析 4GB dump 还行，但 10GB+ 大 dump（生产 32GB 堆）的离线分析工程化不足；MAT `-Xmx` 配置、`--loader` 模式、在线 MAT、`jhat -J-Xmx20g` 等方案的取舍不熟；dump 文件传输（scp 大文件慢 / OSS 中转 / NFS 共享）的工程经验不足
- **架构师水平**：能为团队设计"大 dump 离线分析流水线"（生产 dump -> OSS -> 离线大内存机 -> MAT 自动分析 -> 报告）；能讲清 MAT 处理 dump 时内存与 dump 大小的关系（MAT 内存 = dump × 2-5 倍）；能选择"在线分析 vs 离线分析"的合适场景
- **补足方向**：搭建团队大 dump 分析环境（64GB 内存的离线机）；调研 Datadog / Dynatrace 的在线堆分析产品；调研 Alibaba Arthas heapdump + 在线 MAT 的私有化部署

### 差距3.7：OOM 预防体系建设不足
> Day3发现，延续第4周简历项目差距1.6 / Day1差距1.6

- **现状**：会排查 OOM，但"OOM 预防体系"（Code Review Checklist / 静态扫描 / 监控告警 / 故障演练）的架构师视角不足；缺乏"堆使用率 > 85% 告警"、"Full GC > 1 次/分钟告警"、"Old 区持续上涨不下降告警"等预警机制；缺乏定期故障演练机制
- **架构师水平**：能为团队设计完整的 OOM 预防体系（Checklist + 静态扫描 + 监控告警 + 演练）；能把 JVM 约束写入 Code Review Checklist（如"所有 Caffeine 必须设 maximumSize"、"所有 ThreadLocal 必须 finally remove"）；能设计每月一次的混沌工程演练
- **补足方向**：整理团队"OOM 预防 Checklist"；调研阿里 P3C / SonarQube 的 JVM 相关规则；搭建 Prometheus + JVM Exporter + 告警体系

### 差距3.8：与简历项目 5 个 OOM 场景的结合深度不足
> Day3发现，延续第4周简历项目差距 / Day2差距2.10

- **现状**：能讲 OOM 排查思路，但与简历项目（在线问诊系统）5 个 OOM 场景（IM 网关 ByteBuf 泄漏 / 视频 RTP 包堆积 / 监管上报 OOM / 问诊订单缓存 / MongoDB 大文档 Humongous）的完整 STAR 故事不熟；每个场景的"现象 -> 工具 -> 根因 -> 修复 -> 复盘"5 段式讲述能力不足
- **架构师水平**：能用 STAR 法则结构化讲述 5 个 OOM 案例；能从案例反推架构改进点（如 Caffeine 配置规范、ByteBuf 生命周期管理、Humongous Region 监控）；能在面试中 10 分钟讲清 3 个案例
- **补足方向**：第 2 周 Day05 实战日产出 5 个完整案例文档；用 STAR 法则演练 10 次面试讲述；整理"故障案例库"wiki

---

## 四、CPU 飙高排查实战（Day04）

### 差距4.1：CPU 飙高 6 种类型分类能力不足
> Day4发现，延续第5周差距7.4

- **现状**：知道"CPU 高用 top -Hp + jstack"，但 6 种类型（业务 CPU 高 / GC CPU 高 / JIT 编译 CPU 高 / 锁竞争 CPU 高 / 死循环 CPU 高 / Native CPU 高）的 30 秒分类能力不足；"GC CPU 高本质是内存问题"、"JIT 编译 CPU 高在启动后 5 分钟内"、"锁竞争 CPU 高 jstack 显示 BLOCKED"等快速判断不熟
- **架构师水平**：能 30 秒内根据 top -Hp 线程名（GC Thread / CompileThread / 业务线程 / Native 线程）判断类型；能为每类 CPU 高设计专属排查流程；能讲清"为什么 GC CPU 高调 GC 参数治标不治本"
- **补足方向**：本地装 6 种 CPU 高的复现环境；调研 Netflix FlaPoodle 火焰图产品；阅读 Brendan Gregg 火焰图论文

### 差距4.2：5 步定位法的熟练度与 K8s 容器坑
> Day4发现，延续第2周差距2.9 / Day2差距2.6

- **现状**：知道 `top -Hp PID` + `printf "%x\n" TID` + `jstack -l PID | grep -A 30 nid=0xHEX`，但 K8s 容器内的 5 大坑（top -Hp 不显示线程 / 容器 PID 与宿主机 PID 不一致 / kubectl top 与 exec top 数据不一致 / Pod 内 jps 找不到进程 / 容器内 jstack 权限不足）的工程经验不足；"一键脚本"的能力不足
- **架构师水平**：能不查文档直接写出 5 步法命令链；能背出 K8s 容器内 5 大坑的解决方案；能写出一键脚本 `cpu_hotspot.sh`（自动找进程 -> 找线程 -> 转换 -> 过滤栈）
- **补足方向**：在 K8s 测试集群实战 5 步法 10 次；整理团队"K8s JVM 排查手册"；调研 kubectl debug 一键进入诊断容器

### 差距4.3：async-profiler 4 种模式与火焰图解读不足
> Day4发现，延续第5周差距7.3 / Day2差距2.4

- **现状**：知道 async-profiler 能生成火焰图，但 4 种模式（cpu / alloc / lock / wall）的原理与适用场景不熟；cpu 模式基于 `perf_events` + `AsyncGetCallTrace`、alloc 模式采样内存分配、lock 模式采样锁竞争、wall 模式含 IO 等待等区别不深；火焰图解读能力不足（"宽且尖"vs"宽且平"vs"窄且高"的判断）
- **架构师水平**：能 5 分钟内从火焰图找出 CPU 占 80% 的单一热点方法；能用 `async-profiler -e alloc` 找出内存分配压力大的方法；能用 `-e lock` 找出锁竞争元凶；能讲清火焰图横向（调用栈深度）纵向（采样次数 / CPU 占比）的含义
- **补足方向**：本地装 async-profiler 实战 5 个火焰图；调研 async-profiler 的 `--alloc` 采样频率调节；阅读 Brendan Gregg 火焰图论文

### 差距4.4：JIT 退优化机制与代码反模式识别不足
> Day4发现，延续第1周差距5.7 / Day1差距1.5

- **现状**：知道 JIT 编译概念，但 JIT 编译层次（解释执行 -> C1 -> C2）、退优化的 2 种类型（unstable if 分支预测失效 / not entrant 类加载使假设失效）、uncommon trap 机制不深；从代码分支反推退优化风险（if-else 分支抖动 / 异常控制流 / SPI 动态加载）的能力不足
- **架构师水平**：能用 `-XX:+PrintCompilation` 跟踪 100 个方法的编译历史；能从代码反模式识别退优化风险（如循环内 if 分支抖动）；能用 JFR 持续监控 JIT 退优化事件；能设计预热方案避免冷启动 JIT 抖动
- **补足方向**：阅读 OpenJDK `compileBroker.cpp` / `simpleThresholdPolicy.cpp`；调研 AppCDS / AOT 编译在 JDK 17 的实战；用 JMH 测试预热前后的性能差异

### 差距4.5：Safepoint 与 jstack 关系的深度理解不足
> Day4发现，延续第5周差距7.7

- **现状**：知道 Safepoint 是"JVM 全局安全点"，但 Safepoint 的触发场景（GC / 偏向锁撤销 / delegate disabled / stack watermark）、SafePoint Bias（基于时间的 Safepoint 会让长循环进入慢）、`jstack` / `jcmd Thread.print` 会触发 Safepoint 等细节不深；sync time vs operation time 的区别不熟
- **架构师水平**：能用 `-XX:+PrintSafepointStatistics` + `-XX:+SafepointTimeout` 排查 Safepoint 慢；能讲清"为什么 jstack 在 10w 长连接 IM 网关上会触发 5 秒 STW"（线程多 + 栈深 + safepoint sync 慢）；能识别"长循环无轮询点"反模式
- **补足方向**：阅读 OpenJDK `safepoint.cpp` 源码；调研 JDK 11+ 的"Thread-Local Handshake"（替换全局 Safepoint）；用 JFR 监控 Safepoint 事件

### 差距4.6：CPU 排查工具链副作用的工程意识不足
> Day4发现，延续Day2差距2.7

- **现状**：知道 jstack / Arthas 有副作用，但 `jstack -F` 挂起进程、`Arthas trace` 高 QPS 长挂导致 RT 飙高、`Arthas redefine` 反复试错导致 Metaspace 膨胀、`async-profiler` 采样频率过高导致开销过大等副作用的实战意识不足
- **架构师水平**：能为每个工具建立"副作用清单"与"何时禁用"判断标准；能在生产事故中快速决策"用哪个工具副作用最小"；能用 `monitor` 替代 `watch` 降低开销；能讲清 async-profiler 的采样频率与开销关系（默认 10ms，< 1% 开销）
- **补足方向**：整理团队"工具链副作用速查表"；在测试环境实测 Arthas trace 在 1000 QPS 下的 RT 影响；调研 async-profiler 的 `--interval` 参数调节

### 差距4.7：CPU 飙高预防与配置变更管控不足
> Day4发现，延续第3周 K8s 差距 / Day1差距1.6

- **现状**：会排查 CPU 飙高，但"预防体系"（CPU 使用率告警 / 配置变更管控 / 灰度发布 / JIT 预热）的架构师视角不足；缺乏"CPU > 80% 告警"、"Pod CPU Throttling 监控"、"JIT 编译期监控"等预警机制；缺乏配置变更（如新增 if 分支可能引发退优化）的 Code Review 意识
- **架构师水平**：能为团队设计 CPU 飙高预防体系（监控告警 + 灰度 + 预热 + 演练）；能把 JIT 退优化反模式写入 Code Review Checklist；能设计 JIT 预热方案（如启动后压测流量预热 5 分钟再接生产流量）
- **补足方向**：搭建 Prometheus + CPU Throttling 监控；调研 AppCDS / AOT 编译减少 JIT 开销；整理团队"JIT 退优化反模式 Checklist"

### 差距4.8：与简历项目 5 个 CPU 飙高场景的结合深度不足
> Day4发现，延续第4周简历项目差距 / Day2差距2.10

- **现状**：能讲 CPU 排查思路，但与简历项目（在线问诊系统）5 个 CPU 飙高场景（IM 网关 10w 长连接 ByteBuf 泄漏 / 视频 RTP 包堆积 GC 频繁 / 监管上报 Map 累积 / 问诊订单缓存膨胀 GC 高 / MongoDB 大文档 Humongous GC 高）的完整 STAR 故事不熟
- **架构师水平**：能用 STAR 法则结构化讲述 5 个 CPU 案例；能从案例反推架构改进点（如 ByteBuf 生命周期管理、缓存配置规范、Humongous 监控）；能在面试中 10 分钟讲清 3 个案例
- **补足方向**：第 2 周 Day05 实战日产出 5 个完整案例文档；用 STAR 法则演练 10 次面试讲述；整理"故障案例库"wiki

### 差距4.9：CPU 排查体系建设不足
> Day4发现，延续Day2差距2.8 / 第1周可观测性差距

- **现状**：会使用单个工具，但"CPU 排查体系"（持续火焰图录制 / CPU Throttling 监控 / JIT 退优化告警 / 故障自愈 / 定期演练）的架构师视角不足
- **架构师水平**：能为团队设计完整的 CPU 排查体系（持续录制 + 自动告警 + 故障自愈）；能搭 async-profiler + JFR 持续录制环境；能设计混沌工程演练机制（注入死循环 / 死锁 / 内存泄漏）
- **补足方向**：调研 Datadog APM / Dynatrace 的 CPU 监控产品；搭建 IM 网关的 CPU 排查体系 PoC；调研 Netflix Chaos Monkey 故障注入实践

---

## 五、在线问诊系统 JVM 实战（Day05）

### 差距5.1：5 个 STAR 案例的完整讲述能力不足
> Day5发现，延续第4周简历项目差距 / Day2差距2.10 / Day3差距3.8 / Day4差距4.8

- **现状**：能写 5 个 STAR 案例，但讲述熟练度不足；案例一的 ByteBuf 生命周期、案例二的 G1 Region 选型、案例三的 5 类泄漏模式识别、案例四的 Caffeine 配置、案例五的 G1 Humongous 机制等核心知识点的"5 分钟脱稿讲述"能力不足；面试官追问细节（如"为什么 JDK 8 的 MaxDirectMemorySize 是软限制"、"G1 Region 大小如何选"、"MAT 支配树算法原理"）容易卡壳
- **架构师水平**：能根据面试官兴趣选 2-3 个案例深讲，每个案例 5 分钟内讲完（S 30s + T 10s + A 3min + R 30s）；能在追问中脱口而出底层原理（如 PoolArena / W-TinyLfu / SATB / 染色指针）；能从案例反推架构改进点
- **补足方向**：用 STAR 法则演练 10 次面试讲述（每个案例 2 次）；整理"5 个 STAR 案例面试追问点清单"（每个案例 5-10 个追问）；录音回放改进讲述节奏

### 差距5.2：Direct Memory 排查的工程经验不足
> Day5发现，延续Day1差距1.7 / Day2差距2.4

- **现状**：知道 -XX:MaxDirectMemorySize 但不熟悉 NMT（Native Memory Tracking）的实战使用；Direct Memory 监控盲区（4 维度监控只看 Heap，不看 Direct / Metaspace / RSS）；Arthas 查 DirectByteBuffer 实例的命令不熟（`vmtool --action getInstances --className java.nio.DirectByteBuffer`）；-XX:+HeapDumpOnOutOfMemoryError 抓不到 Direct 现场的陷阱认识不足
- **架构师水平**：能 30 秒内用 `jcmd VM.native_memory summary` 看 Internal 分类；能用 Arthas `ognl '@java.nio.Bits@reservedMemory'` 查 Direct 占用；能讲清 DirectByteBuffer / Cleaner / PhantomReference 的关系；能为团队设计 4 维度监控（Heap / Direct / Metaspace / RSS）
- **补足方向**：每周练 1 次 NMT 分析；搭建 Prometheus + JVM Exporter 4 维度监控；阅读 OpenJDK `heapDumper.cpp` 源码理解 HeapDump 局限

### 差距5.3：G1 调优参数耦合关系理解不足
> Day5发现，延续第5周差距7.4 / Day1差距1.2

- **现状**：知道 G1 Region / IHOP / G1ReservePercent / MaxGCPauseMillis 各参数含义，但参数间的耦合关系（如 Region 大小要匹配业务对象大小、IHOP 调低让并发标记更早启动、G1ReservePercent 防 to-space exhausted）的工程经验不足；G1 Humongous Object 触发条件（对象 >= Region / 2）记不熟；G1 Mixed GC 失败原因（CSet 选择失败 / RSet 更新成本高 / to-space exhausted）讲不清
- **架构师水平**：能根据业务对象大小选 Region（Region >= 业务最大对象 × 2）；能根据 Old 区上涨速度调 IHOP（35-45%）；能为不同业务场景设计 G1 调优参数模板（IM / 视频 / 监管 / 归档 4 套）；能从 GC 日志反推哪个参数需要调整
- **补足方向**：精读 OpenJDK `g1CollectedHeap.cpp` 源码；调研 Netflix / LinkedIn 的 G1 调优案例；在测试环境实测 Region 4MB vs 8MB vs 16MB 的 GC 表现

### 差距5.4：MAT 支配树与 OQL 的深度不足
> Day5发现，延续Day2差距2.3 / Day3差距3.3

- **现状**：会用 MAT 看 Histogram / Dominator Tree / Leak Suspects 三大视图，但支配树 Dominator Tree 的算法原理（X 支配 Y 当且仅当所有从 GC Roots 到 Y 的路径都经过 X）讲不清；OQL 语法（`SELECT s FROM java.util.HashMap s WHERE s.@retainedHeapSize > 1000000`）写不熟；Path to GC Roots vs Merge Shortest Paths 的适用场景不熟；Allocation Record 看对象创建栈的能力不足
- **架构师水平**：能讲清支配树的"支配集"定义；能 1 分钟内从 Dominator Tree 找到 Retained Top 1 元凶；能用 OQL 查询特定模式的对象（如所有 size > 10000 的 HashMap）；能用 Allocation Record 反推对象创建栈
- **补足方向**：精读 Eclipse MAT 官方文档 + IBM Dominator Tree paper；每周练 2 个 OQL 查询；本地装 MAT 实战 3 个真实 dump

### 差距5.5：5 类内存泄漏模式识别清单不熟
> Day5发现，延续Day3差距3.4

- **现状**：知道缓存膨胀和 ThreadLocal，但 5 类模式（缓存膨胀 / ThreadLocal 累积 / 监听器 Callback 未 unregister / 静态集合无限增长 / finalize 队列堆积）的典型代码、识别清单、修复方案不熟；Code Review 时不能快速识别"线程池 + ThreadLocal"、"静态 Map + UUID key"等反模式；finalize 为什么被 JDK 9 标记 Deprecated、JDK 18 提案移除的原因不深
- **架构师水平**：能背出 5 类泄漏模式的识别清单（如 Caffeine 是否设了 maximumSize / ThreadLocal 是否在 finally 中 remove / 监听器是否在不需要时 unregister / 静态 Map 是否有 TTL 和清理 / 是否用了 finalize）；能从 Code Review 中识别反模式；能讲清 Cleaner（JDK 9+）替代 finalize 的原理
- **补足方向**：整理团队"5 类泄漏模式识别 Checklist"（10 条 Code Review 规则）；调研阿里 P3C 静态扫描规则；在测试环境复现 5 类泄漏各一次

### 差距5.6：Caffeine 与缓存架构的深度不足
> Day5发现，延续第4周简历项目差距

- **现状**：知道 Caffeine 基本用法（maximumSize + TTL + recordStats），但 W-TinyLfu 算法原理（Window LRU + Count-Min Sketch 估算频率）、Caffeine vs Guava Cache vs Ehcache 的区别、Caffeine vs Redis 的边界划分（1w Key 以内用 Caffeine，超过用 Redis）、两级缓存查询逻辑（Caffeine 未命中查 Redis + 回填）的工程经验不足
- **架构师水平**：能讲清 W-TinyLfu 算法（比 LRU 命中率高 30%+）；能为不同业务场景设计三级缓存配比（Caffeine / Redis / MySQL）；能用 Caffeine stats 暴露 4 维度监控（size / hit rate / eviction / load time）；能设计容量规划公式（QPS × TTL = maximumSize）
- **补足方向**：精读 Caffeine wiki + W-TinyLfu 论文；整理团队"缓存选型规范"；搭建 Caffeine + Prometheus 监控 PoC

### 差距5.7：JVM 与业务架构协同的全局视角不足
> Day5发现，延续Day1差距1.7 / 第4周简历项目差距1.5

- **现状**：能讲 JVM 调优参数和业务架构，但"JVM 与业务架构协同"的 5 个维度（缓存架构 / 消息可靠性 / 大对象处理 / 长连接管理 / 容量规划）的全局视角不足；K8s limit / JVM Heap / Direct Memory / Metaspace 的全局配比模板不熟；10w 长连接的内存模型（每连接 4KB Direct + 200B Heap）算不清
- **架构师水平**：能为 4C8G / 8C16G / 16C32G Pod 设计标准 JVM 内存配比模板；能用 NMT 精确测算 JVM 自身开销；能为在线问诊系统 4 个服务设计 4 套生产配置（IM / 视频 / 监管 / 归档）；能讲清长连接管理与 GC 停顿的协同（MaxGCPauseMillis < 心跳超时）
- **补足方向**：用 `jcmd PID VM.native_memory summary` 实测 IM 网关的 JVM 内存构成；调研 Netflix 容量规划方法论；为在线问诊系统设计 3 套标准 Pod 配比

### 差距5.8：MongoDB Schema 与冷热分离的工程经验不足
> Day5发现，延续第4周简历项目差距

- **现状**：知道 MongoDB 大文档会触发 G1 Humongous，但 MongoDB Schema 设计原则（避免大文档 / 图片存 OSS / 大文档拆分 / 冷热分离）的工程经验不足；冷热分离实现方案（MongoDB 6 个月内 + OSS 6 个月后 + Hive 元数据同步）不熟；ES Lucene FST 用 MMAP 解决大对象内存压力的对比不深
- **架构师水平**：能讲清 MongoDB Schema 设计 4 原则（避免大文档 / 图片存 OSS / 大文档拆分 / 冷热分离）；能设计完整归档流水线（MongoDB -> OSS -> Hive）；能对比 MongoDB 大文档 vs ES Lucene FST 的内存压力与解决方案
- **补足方向**：调研 MongoDB Schema 设计最佳实践；实战 OSS 归档 + Hive 元数据同步；对比 ES Lucene MMAP 机制

---

## 六、串联整合-故障复盘方法论（Day06）

### 差距6.1：5 分钟止血的节奏感不足
> Day6发现，延续Day2差距2.6 / Day3差距3.5 / Day4差距4.2

- **现状**：知道"5 分钟定位"思路，但实际生产中 5 个节奏点（0-1 分钟现象确认 / 1-2 分钟止损决策 / 2-3 分钟抓现场 / 3-4 分钟临时止血 / 4-5 分钟稳定确认）的节奏感不足；止损决策（重启 / 摘流量 / 限流 / 降级）的判断标准不清晰；"先抓 dump 还是先摘流量"的权衡（先摘流量避免雪崩，再抓 dump）容易选错
- **架构师水平**：能背出 5 个节奏点的关键动作；能在 30 秒内做止损决策（P0 立即重启 / P1 摘流量抓现场）；能 1 分钟内抓完 dump + GC 日志 + jstack 三件套；能为不同故障类型设计止血策略（OOM / CPU 高 / GC 频繁 / 死锁 / 死循环）
- **补足方向**：每月一次故障演练（注入内存泄漏 / CPU 死循环 / 死锁）；整理团队"5 分钟止血 SOP"（5 个节奏点 + 决策树）；用 STAR 法则整理 5 个真实案例

### 差距6.2：根因 6 层次分析的能力不足
> Day6发现，延续Day3差距3.5 / Day4差距4.9

- **现状**：能定位代码根因，但"根因 6 层次"（现象 / 直接 / 代码 / 配置 / 流程 / 体系）的彻底分析能力不足；通常停留在第 3 层代码根因，第 4 层配置根因（JVM + Kafka + K8s 跨层）、第 5 层流程根因（变更管理 / 监控告警 / Code Review）、第 6 层体系根因（能力 / 工具 / 演练 / 容量 / 知识库）的深挖能力不足
- **架构师水平**：能为每个故障挖到第 6 层体系根因；能讲清"代码根因只是症状，体系根因才是病因"；能从单次故障反推 5 个体系建设（能力建设 / 工具链 / 演练 / 容量规划 / 知识库）；能避免同类故障重演
- **补足方向**：每次复盘挖到第 6 层；整理团队"根因 6 层次模板"；调研 Google SRE 故障复盘方法论

### 差距6.3：改进项分层规划与跟踪闭环不足
> Day6发现

- **现状**：能列出改进项，但"分层规划"（P0 24 小时 / P1 1 周 / P2 1 个月 / P3 3 个月）的优先级划分不清；改进项跟踪闭环（复盘会认领 -> 每周例会跟踪 -> 月度统计 -> 架构师 Review 验收）的机制不熟；改进项验收标准（代码层 / 配置层 / 流程层 / 体系层）的差异化设计不足
- **架构师水平**：能为每个改进项划分 P0/P1/P2/P3 优先级；能设计完整的跟踪闭环机制（Jira 跟踪 + 每周例会 + 月度统计 + 验收标准）；能为不同层改进项设计验收标准；能保证改进项完成率 90%+
- **补足方向**：建立 Jira 改进项跟踪机制；整理团队"改进项分层模板"；月度统计完成率

### 差距6.4：跨层根因分析能力不足
> Day6发现，延续Day1差距1.4 / Day2差距2.9 / 第3周 K8s 差距

- **现状**：能看代码根因，但跨层根因分析（JVM + K8s + Kafka + MongoDB 联动）的能力不足；K8s cgroup v2 与 JDK 8u362 兼容问题（CPU 限制识别不准）、Kafka 消费者参数（max.poll.records / fetch.max.bytes）对 JVM 的影响、K8s limit 与 JVM Heap / Direct / Metaspace 的配比等跨层知识不深
- **架构师水平**：能讲清 K8s cgroup v1 vs v2 与 JDK 版本兼容矩阵；能讲清 Kafka 消费者参数对 JVM 内存压力的影响；能为 K8s Pod 设计标准 JVM 内存配比模板（Heap 50% / Direct 25% / Metaspace 6% / JVM 自身 13% / Buffer 6%）
- **补足方向**：学习 K8s 故障排查（cgroup / kubelet / etcd）；学习 Kafka 故障排查（consumer lag / rebalance）；整理"K8s + JVM 协同排查手册"

### 差距6.5：故障演练与混沌工程经验不足
> Day6发现，延续Day2差距2.8 / Day4差距4.9

- **现状**：知道"故障演练"概念但没实战过；混沌工程工具（Netflix Chaos Monkey / 阿里 ChaosBlade）不熟；注入故障类型（内存泄漏 / CPU 死循环 / 死锁 / Direct 泄漏 / Full GC 频繁）的演练设计不足；演练后的复盘改进机制不熟
- **架构师水平**：能设计每月一次的混沌工程演练机制；能用 ChaosBlade 注入 5 类故障（内存泄漏 / CPU 死循环 / 死锁 / Direct 泄漏 / Full GC 频繁）；能从演练中沉淀 SOP；能为团队建立"故障案例库"wiki
- **补足方向**：搭建 ChaosBlade 演练环境；每月一次故障演练；调研 Netflix Chaos Monkey 实践

### 差距6.6：变更管理与监控告警的流程完善不足
> Day6发现，延续Day1差距1.6 / Day3差距3.7

- **现状**：知道变更管理流程，但"变更评估必须包含 JVM 容量评估"的工程意识不足；监控告警阈值不合理（80% P2 告警但不主动 dump，95% 才主动 dump 已 OOM）；Code Review Checklist 不全（5 类泄漏模式 Checklist 没有覆盖"静态 Map + UUID key"模式）；变更后观察期不足（24 小时看不出慢泄漏）
- **架构师水平**：能把 JVM 容量评估写入变更管理流程；能设计 80% 主动 dump + 90% 主动摘流量的告警机制；能完善 Code Review Checklist（10 条 JVM 安全规则）；能为大变更设计 7 天观察期 + 每天对比 JVM 指标趋势
- **补足方向**：整理团队"变更管理 JVM 评估模板"；搭建 80% 主动 dump 机制；完善 Code Review Checklist

### 差距6.7：复盘文档标准化与知识沉淀不足
> Day6发现

- **现状**：能写复盘文档，但标准化程度不足（结构不统一 / 时间线不清晰 / 根因层次不够深 / 改进项无跟踪）；复盘文档存个人电脑，团队看不到；故障案例库不完善，同类故障重演
- **架构师水平**：能用标准模板写复盘文档（7 部分：概述 / 时间线 / 影响 / 根因 6 层次 / 改进项 / 经验教训 / 附录）；能把复盘文档 wiki 化；能建立团队"故障案例库"分类整理（按故障类型 / 服务 / 根因层次）；能用案例库做团队培训
- **补足方向**：整理团队"复盘文档标准模板"；搭建团队"故障案例库"wiki；每月分享 1 个历史故障案例

### 差距6.8：架构师视角的复盘能力体系建设不足
> Day6发现，延续Day2差距2.8 / Day4差距4.9

- **现状**：能做单次复盘，但"复盘能力体系建设"（5 分钟止血 SOP / 根因 6 层次模板 / 改进项分层 / 跟踪闭环 / 知识沉淀 / 演练机制）的架构师视角不足；团队整体复盘能力不足（依赖 SRE 排查，研发不会用 MAT）；复盘反模式（甩锅式 / 浅尝辄止式 / 改进项不跟踪 / 只修代码不修体系 / 文档不沉淀）认识不足
- **架构师水平**：能为团队设计完整的复盘能力体系（SOP + 模板 + 跟踪 + 演练 + 案例库）；能识别 5 类复盘反模式并改进；能培训团队 JVM 排查能力（每月培训 + 演练）；能从单次故障反推 5 个体系建设
- **补足方向**：整理团队"复盘能力体系建设 RFC"；调研 Google SRE / Netflix Chaos 工程实践；3 个月内建立完整复盘能力体系

---

## 七、本周差距汇总与补足路径

### 7.1 差距分类统计

| 主题 | 差距数 | 关键差距 |
|------|-------|---------|
| JVM 调优参数全解（Day01） | 7 | 版本演进 / MaxGCPauseMillis 陷阱 / 指针压缩 / 容器化 / JIT 协同 / -XX 陷阱 / 内存规划 |
| JVM 诊断工具链实战（Day02） | 10 | 工具版本差异 / Arthas 熟练度 / MAT 深度 / 火焰图 / GC 日志 / 5分钟定位 / 副作用 / 工具体系 / K8s 工具 / 实战案例 |
| OOM 排查实战（Day03） | 8 | OOM 类型识别 / Dump 方式选择 / MAT 支配树 / 5 类泄漏模式 / 5 分钟定位 / 大 dump 处理 / OOM 预防体系 / 简历项目案例 |
| CPU 飙高排查实战（Day04） | 9 | 6 种类型分类 / 5 步法熟练度 / async-profiler 4 模式 / JIT 退优化 / Safepoint / 工具副作用 / CPU 预防体系 / 简历项目案例 / 排查体系 |
| 在线问诊系统 JVM 实战（Day05） | 8 | 5 个 STAR 讲述 / Direct 排查 / G1 调优耦合 / MAT 支配树 OQL / 5 类泄漏模式 / Caffeine 缓存 / 业务架构协同 / MongoDB Schema |
| 串联整合-故障复盘方法论（Day06） | 8 | 5 分钟止血节奏 / 根因 6 层次 / 改进项分层跟踪 / 跨层根因 / 故障演练 / 变更管理 / 复盘文档标准化 / 复盘能力体系 |
| **本周合计** | **50** | 累计 117+ 项 + 本周新增 50 项 = **累计 167+ 项** |

### 7.2 高优先级补足方向（本周剩余 + 后续）

#### 优先级 1：生产实战工具链体系建设
- 差距2.1 / 2.2 / 2.5 / 2.8 / 3.3 / 4.3 / 4.6 / 5.4（工具链 + MAT + async-profiler + 副作用）

**补足路径**：
- 每日练 1 个工具（周一 jstack / 周二 Arthas / 周三 MAT / 周四 JFR / 周五 async-profiler）
- Day05 已整合 5 个 STAR 案例
- 搭建团队 JFR 长开 + Arthas sidecar 体系

#### 优先级 2：5 分钟定位法实战演练 + 故障复盘方法论
- 差距2.6 / 2.7 / 3.5 / 4.2 / 6.1 / 6.2 / 6.5（生产事故节奏 + K8s 容器坑 + 5 分钟止血 SOP + 根因 6 层次）

**补足路径**：
- 每月一次故障演练（注入内存泄漏 / CPU 死循环 / 死锁）
- 整理团队"5 分钟止血 SOP"（5 个节奏点 + 决策树）
- 整理团队"根因 6 层次模板"
- 用 STAR 法则整理 5 个真实案例

#### 优先级 3：JDK 8 -> 17 升级工程化
- 差距1.1 / 1.4 / 3.1 / 4.4 / 5.3（参数版本演进 + JIT 退优化 + G1 调优耦合）

**补足路径**：
- 本周末写一份 IM 网关 JDK 17 升级 RFC
- 用 `jdeps` 全量扫描失效参数
- 在测试环境跑一次完整参数迁移
- 调研 AppCDS / AOT 编译减少 JIT 开销
- Day07 深挖 ZGC 后评估视频问诊 SFU 切换 ZGC

#### 优先级 4：JVM 调优与业务架构协同
- 差距1.6 / 1.7 / 3.7 / 4.7 / 5.6 / 5.7（生产配置模板 + 预防体系 + 缓存架构 + 业务协同）

**补足路径**：
- Day05 已整理 JVM 生产配置模板库（IM / 视频 / 监管 / 归档 4 套）
- 把 JVM 约束写入 Code Review Checklist（Caffeine maximumSize / ThreadLocal remove / 监听器 unregister）
- 调研 Google SRE"性能预算"实践
- 用 NMT 实测 IM 网关的 JVM 内存构成

#### 优先级 5：简历项目实战案例 + 故障案例库
- 差距2.10 / 3.8 / 4.8 / 5.1 / 5.5 / 6.7（在线问诊系统 5 个场景 + STAR 讲述 + 案例库）

**补足路径**：
- Day05 已产出"简历项目-在线问诊系统-JVM调优案例.md"（5 个完整 STAR 故事）
- 用 STAR 法则演练面试讲述（OOM 3 个 + CPU 2 个）
- 整理"故障案例库"wiki
- 录音回放改进讲述节奏

#### 优先级 6：OOM + CPU 预防体系 + 复盘能力体系
- 差距3.7 / 4.7 / 4.9 / 6.3 / 6.6 / 6.8（预防体系 + 排查体系 + 复盘能力体系）

**补足路径**：
- 搭建 Prometheus + JVM Exporter + 告警体系（4 维度：Heap / Direct / Metaspace / RSS）
- 设计每月一次混沌工程演练（ChaosBlade）
- 整理团队"复盘能力体系建设 RFC"
- 调研 Datadog APM / Dynatrace 的 JVM 监控产品

### 7.3 跨周延续差距追踪

| 差距 | 来源 | 延续到本周 | 第 2 周是否补足 |
|------|------|----------|--------------|
| JDK 8 -> 17 升级 | 第4周简历项目 | Day1/Day3/Day4/Day5延续 | 第 2 周末写 RFC（Day07 评估 ZGC） |
| K8s 容器化协同 | 第3周 K8s | Day1/Day2/Day4/Day5/Day6延续 | Day06 跨层根因分析（cgroup v2 兼容） |
| 全链路可观测性 | 第1周 | Day2/Day5延续 | Day05 4 维度监控 + Day06 监控告警 |
| 简历项目实战 | 第4周简历项目 | Day2/Day3/Day4/Day5延续 | Day05 已产出 5 个 STAR 案例 |
| G1 调优与参数耦合 | 第5周 Day7 | Day1/Day3/Day5延续 | Day05 实战 5 个案例的 G1 调优 |
| JFR + async-profiler | 第5周 Day7 | Day2/Day4延续 | Day05 4 维度监控 + JFR 持续录制 |
| 生产故障排查 | 第5周 Day7 | Day2/Day3/Day4/Day5/Day6延续 | Day06 完整复盘方法论 |
| JIT 退优化排查 | 第1周 | Day1/Day4延续 | Day04 JIT 退优化 |
| 5 类泄漏模式 | Day3 | Day5延续 | Day05 案例三命中"静态集合无限增长" |
| Direct Memory 排查 | Day1/Day2 | Day5延续 | Day05 案例一 NMT + Arthas 实战 |
| 5 分钟定位法 | Day2/Day3/Day4 | Day6延续 | Day06 5 分钟止血 SOP |

### 7.4 第 2 周剩余补足计划

```text
第 2 周（2026年08月第1周）：JVM 专题第 2 周 - 调优实战与生产排查

Day01（已完成）：JVM 调优参数全解
                - 补足差距1.1-1.7（参数体系）
                - 堆 / GC / JIT / 容器化 / -XX 陷阱 / 生产模板

Day02（已完成）：JVM 诊断工具链实战
                - 补足差距2.1-2.10（工具链）
                - jps/jstat/jmap/jstack/jcmd + Arthas + MAT + async-profiler + GC 日志

Day03（已完成）：OOM 排查实战
                - 补足差距3.1-3.8（OOM 排查体系）
                - 8 种 OOM 类型 / 5 种 Heap Dump 方式 / MAT 支配树 / 5 类泄漏模式 / 5 分钟定位

Day04（已完成）：CPU 飙高排查实战
                - 补足差距4.1-4.9（CPU 排查体系）
                - 6 种 CPU 高类型 / 5 步法 / async-profiler 4 模式 / JIT 退优化 / Safepoint

Day05（已完成）：在线问诊系统 JVM 调优案例
                - 补足差距5.1-5.8（实战案例 + 业务协同）
                - 5 个 STAR 案例 + JVM 调优参数模板 + 业务架构协同
                - 产出简历项目延伸文档：在线问诊系统-JVM调优案例.md

Day06（已完成）：串联整合 - 故障复盘方法论
                - 补足差距6.1-6.8（复盘能力体系）
                - 故障时间线 / 根因 6 层次 / 复盘模板 / 改进项跟踪 / 5 分钟止血 SOP

Day07（待出）：架构深挖 - ZGC 底层原理
              - 补足差距1.1 / 5.3 / 第5周差距3.2 / 7.7（ZGC / Shenandoah）
              - 染色指针 / 读屏障 / 并发整理 / 分代 ZGC（JDK 21）
```

### 7.5 本周差距关键词云

```text
高频关键词（出现 3 次以上）：
  - JVM 诊断工具链（7 次）：差距2.1 / 2.2 / 2.5 / 2.7 / 2.8 / 4.6 / 5.4
  - 生产实战节奏（9 次）：差距2.6 / 2.7 / 2.10 / 1.6 / 3.5 / 4.2 / 4.8 / 6.1 / 6.5
  - 5 分钟定位法（6 次）：差距2.6 / 2.7 / 3.5 / 4.2 / 6.1 / 跨周延续
  - 简历项目实战（6 次）：差距1.7 / 2.10 / 3.8 / 4.8 / 5.1 / 跨周延续
  - JDK 版本演进（4 次）：差距1.1 / 1.4 / 2.1 / 5.3
  - K8s 容器化协同（4 次）：差距1.4 / 2.9 / 4.2 / 6.4
  - JIT 与编译体系（3 次）：差距1.5 / 4.4 / 跨周延续
  - 工具链体系建设（4 次）：差距2.8 / 2.9 / 4.9 / 6.8
  - 根因分析深度（5 次）：差距3.5 / 4.9 / 5.4 / 6.2 / 6.4
  - 复盘能力体系（4 次）：差距6.2 / 6.3 / 6.7 / 6.8
  - 预防体系建设（3 次）：差距3.7 / 4.7 / 6.6

中频关键词（出现 2 次）：
  - 参数与版本绑定（2 次）：差距1.1 / 1.4
  - GC 调优参数耦合（3 次）：差距1.2 / 1.5 / 5.3
  - JFR + async-profiler（2 次）：差距2.4 / 4.3
  - MAT 深度分析（3 次）：差距2.3 / 3.3 / 5.4
  - 火焰图解读（2 次）：差距2.4 / 4.3
  - OOM 类型识别（2 次）：差距3.1 / 跨周延续
  - 内存泄漏模式（3 次）：差距3.4 / 5.5 / 跨周延续
  - Safepoint + JIT 退优化（2 次）：差距4.4 / 4.5
  - Direct Memory 排查（3 次）：差距1.7 / 5.2 / 跨周延续
  - Caffeine 缓存（2 次）：差距5.6 / 跨周延续
  - 业务架构协同（2 次）：差距1.7 / 5.7
  - 改进项跟踪闭环（2 次）：差距6.3 / 6.8
  - 故障演练（3 次）：差距4.9 / 6.5 / 6.8

低频关键词（出现 1 次）：
  - MaxGCPauseMillis 陷阱（1 次）：差距1.2
  - 指针压缩 32GB 边界（1 次）：差距1.3
  - JVM 内存规划全局视角（1 次）：差距1.7
  - Arthas 高阶命令（1 次）：差距2.2
  - GC 日志分析（1 次）：差距2.5
  - K8s 工具链接入（1 次）：差距2.9
  - Heap Dump 方式选择（1 次）：差距3.2
  - 大 dump 离线分析（1 次）：差距3.6
  - CPU 6 种类型分类（1 次）：差距4.1
  - 工具链副作用（1 次）：差距4.6
  - 5 个 STAR 讲述（1 次）：差距5.1
  - MongoDB Schema（1 次）：差距5.8
  - 变更管理流程（1 次）：差距6.6
  - 复盘文档标准化（1 次）：差距6.7
```

---

## 八、附录：本周学习节奏回顾

```text
2026年08月第1周（JVM 专题第 2 周 - 调优实战与生产排查）

Day01（08/03 周一）：JVM 调优参数全解
  - 出题 + 作答 + 梳理
  - 5 个子问题：堆内存 / GC / JIT / 容器化 / -XX 陷阱
  - 发现 7 项差距（1.1-1.7）

Day02（08/04 周二）：JVM 诊断工具链实战
  - 出题 + 作答 + 梳理
  - 5 个子问题：JDK 自带 / Arthas / MAT / jstack+火焰图 / GC 日志
  - 1 个场景题：5 分钟定位 Full GC 元凶
  - 发现 10 项差距（2.1-2.10）

Day03（08/05 周三）：OOM 排查实战
  - 出题 + 作答 + 梳理
  - 5 个子问题：8 种 OOM 类型 / 5 种 dump 方式 / MAT 支配树 / 5 类泄漏模式 / 5 分钟定位
  - 1 个场景题：监管上报服务 OOM 排查（STAR 法则）
  - 发现 8 项差距（3.1-3.8）

Day04（08/06 周四）：CPU 飙高排查实战
  - 出题 + 作答 + 梳理
  - 5 个子问题：6 种 CPU 类型 / 5 步法 / async-profiler 4 模式 / JIT 退优化 / Safepoint
  - 1 个场景题：IM 网关 CPU 95% 排查（STAR 法则）
  - 发现 9 项差距（4.1-4.9）

Day05（08/07 周五，补全）：在线问诊系统 JVM 实战
  - 出题 + 作答 + 梳理
  - 5 个 STAR 案例：IM 网关 ByteBuf OOM / 视频 RTP 包堆积 / 监管上报 Map OOM / 问诊订单缓存膨胀 / MongoDB 大文档 Humongous
  - JVM 调优参数模板（4 套生产配置）
  - JVM 与业务架构协同（5 个维度）
  - 产出简历项目延伸文档：在线问诊系统-JVM调优案例.md
  - 发现 8 项差距（5.1-5.8）

Day06（08/08 周六，今日）：串联整合 - 故障复盘方法论
  - 故障时间线（5 分钟节奏点）+ 根因 6 层次分析 + 复盘文档模板 + 改进项跟踪闭环
  - 5 分钟止血 SOP（5 个节奏点 + 不同故障类型的止血策略）
  - 架构师视角的复盘能力（5 个核心能力 + 5 类反模式）
  - 发现 8 项差距（6.1-6.8）

Day07（08/09 周日，明日）：架构深挖 - ZGC 底层原理
  - 染色指针 / 读屏障 / 并发整理 / 分代 ZGC（JDK 21）
```

---

## 九、与简历项目的衔接

本周 Day02-Day06 已与简历项目（在线问诊系统）的 5 个场景深度结合，形成"工具链 -> OOM 排查 -> CPU 排查 -> 实战案例 -> 故障复盘"的完整闭环：

| 场景 | Day02 工具链切入 | Day03 OOM 排查切入 | Day04 CPU 飙高切入 | Day05 STAR 故事 | Day06 复盘视角 |
|------|----------------|------------------|------------------|---------------|--------------|
| IM 网关 10w 长连接 | top -Hp + jstack + async-profiler | Direct Buffer OOM 雏形 | ByteBuf 泄漏检测风暴 CPU 95% | 案例一：ByteBuf 直接内存 OOM | Direct Memory 4 维度监控 |
| 视频问诊 RTP 包堆积 | jmap dump + MAT Dominator Tree | RTPQueue 未 clear 引发 OOM | GC 频繁 CPU 高 | 案例二：RTP 包堆积 Full GC | G1 Region 选型 + ZGC 评估 |
| 监管上报 24h 必达 | OOM 自动 dump + MAT | ConcurrentHashMap 累积 OOM（场景题） | Map 累积引发 GC CPU 高 | 案例三：Map 累积 heap OOM | 根因 6 层次（代码 + 配置 + JVM + K8s + 变更 + 体系） |
| 问诊订单缓存 100w Key | Arthas vmtool + ognl | Caffeine 缓存膨胀 OOM | GC CPU 高 | 案例四：缓存膨胀 GC CPU 高 | Caffeine 配置规范 + Code Review Checklist |
| MongoDB 大文档 G1 Humongous | GC 日志 + jmap -histo | Humongous Region OOM | GC CPU 高 | 案例五：Humongous Allocation | MongoDB Schema 优化 + 冷热分离 |

**Day03 场景题**：监管上报服务 OOM 排查（完整 STAR 法则 - 5 层根因分析）
**Day04 场景题**：IM 网关 CPU 95% 排查（完整 STAR 法则 - ByteBuf 泄漏检测 PARANOID 风暴）
**Day05 实战日**：5 个完整 STAR 故事 + JVM 调优参数模板（4 套生产配置）+ JVM 与业务架构协同（5 个维度）
**Day06 串联日**：一次完整的 JVM 故障复盘（5 分钟止血 SOP + 根因 6 层次 + 复盘模板 + 改进项跟踪）

**Day05 已产出** `2026年07月第4周/简历项目-在线问诊系统-JVM调优案例.md`，作为简历项目打磨的核心产出，把 5 个场景的 Day02-Day04 排查过程整合成 5 个完整的 STAR 故事，与已有 3 份简历文档（架构文档 / 难点拆解 / 面试预演）形成"架构 -> 难点 -> 面试预演 -> JVM 调优"完整闭环。

**Day06 复盘方法论**将 5 个 STAR 案例的"单点排查"提升为"完整复盘能力体系"（5 分钟止血 SOP + 根因 6 层次模板 + 改进项分层跟踪 + 故障演练机制 + 知识库沉淀），是架构师视角才能真正驾驭的能力。
