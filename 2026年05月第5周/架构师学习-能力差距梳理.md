# 架构师面试能力差距梳理 — 第5周服务治理

## 当前定位：Java高级开发 → 目标：架构师

### 第5周Day2问答表现基准：60/100
- 服务治理的整体概念有，但细节深度不够
- 实战经验（线程池隔离第三方调用）是亮点
- 核心短板：熔断策略不熟、漏斗结构思维缺失、售罄标记未掌握

---

## 差距一：熔断策略对应故障模式 — 知识空白
> Day2发现

### 现状
完全不了解Sentinel三种熔断策略（慢调用比例/异常比例/异常数），无法区分适用场景。

### 架构师为什么需要
熔断是容错的核心机制，面试必问。不知道三种策略的区别 = 没有深度使用过Sentinel。面试官一追问"你线上熔断规则怎么配的"，答不上来直接减分。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 加个熔断，失败了走降级 |
| 高级开发 | 熔断器有CLOSED/OPEN/HALF_OPEN三种状态 |
| 架构师 | 三种策略对应三种故障模式：慢调用比例→下游变慢但没挂（DB慢查询）、异常比例→偶发异常（第三方API不稳定）、异常数→必挂（服务OOM）。参数调优：timeWindow看下游恢复时间（业务10s/支付30s/DB故障60s），minRequestAmount防误杀取5-10 |

### 怎么补
1. **记住对应关系**：慢调用管"变慢"、异常比例管"偶尔挂"、异常数管"必挂"
2. **写三组规则配置**：针对你项目中的订单服务、支付服务、库存服务各写一组熔断规则，标注每个参数为什么这么设
3. **面试话术**："我线上配的是慢调用比例熔断，因为订单服务更多是慢查询而不是直接挂掉，RT阈值500ms、比例50%、熔断10秒"

---

## 差距二：漏斗结构思维缺失 — 罗列式回答
> Day2发现

### 现状
答容错时罗列了CDN、网关、MQ、缓存、限流、监控等措施，但没有"每层挡多少"的定量结构。答五层防御时说成"前端静态加速+网关负载均衡+MQ削峰+限流+监控"，像在报菜名。

### 架构师为什么需要
面试官听容错设计，想听的是**层次感和数字**，不是措施清单。"五层漏斗，每层挡90%，从10万QPS漏到DB只有1次写入"比"我们有限流、熔断、降级"强十倍。漏斗模型体现的是对系统全链路的量化掌控。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 我们有限流和降级 |
| 高级开发 | 网关限流+服务层熔断+MQ削峰，多层防御 |
| 架构师 | 五层漏斗：L1 CDN挡90%恶意流量→L2网关令牌桶挡90%溢出→L3服务层Redis预扣挡90%无效→L4熔断降级挡90%异常→L5 DB兜底。10万QPS→1万→1000→100→10→1。每层限流算法不同：令牌桶(入口)→滑动窗口(中间)→熔断(保护)→漏桶(出口) |

### 怎么补
1. **每次答容错题，先画漏斗图**：标出每层的通过量，用数字说话
2. **记住"每层挡90%"的经验值**：不是精确数字，但体现架构师的量化思维
3. **面试时主动说**："从容错角度看是漏斗，从算法角度看每层不同：入口令牌桶、中间滑动窗口、出口漏桶"

---

## 差距三：售罄标记机制未掌握
> Day2发现

### 现状
提到了Lua+Redis防超卖，但没想到"卖完之后怎么办"。售罄后的流量处理是比限流更强力的优化，完全没想到。

### 架构师为什么需要
防超卖是业务正确性，售罄标记是高可用+业务正确性的交汇点。秒杀5秒卖完，剩余55秒请求全部直接返回——**DB压力归零**。这是面试中的亮点回答，比"限流1万QPS"更有说服力。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | Lua脚本防超卖 |
| 高级开发 | Redis预扣库存+Lua原子操作+数据库对账 |
| 架构师 | 防超卖是业务正确性，售罄标记是性能优化。库存扣到0时设Redis soldout标记，后续请求直接返回"已售罄"，零DB压力。秒杀5秒卖完→剩余55秒DB写入量归零。防超卖+售罄标记 = 正确性+性能的双重保障 |

### 怎么补
1. **画时间线**：10:00开抢→10:00:05售罄→10:00:05设置标记→10:01:00全部直接返回。对比有无标记的DB压力曲线
2. **面试话术**："我们最强优化不是限流，是售罄标记。卖完后DB压力直接归零，比限流更彻底"
3. **补充细节**：售罄标记要设过期时间（EX 3600），避免活动结束后标记残留

---

## 差距四：滑动窗口选型根因不清晰
> Day2发现

### 现状
答"Sentinel为什么选滑动窗口"时说了"实现简单、轻量级"，但没说到根本原因——**多维度统计**。

### 架构师为什么需要
"实现简单"是表面原因，面试官一追问"令牌桶实现也不复杂啊，为什么不选？"就答不上来了。根本原因是：令牌桶只能统计"令牌数"一个维度，但熔断器需要异常比例、慢调用比例、RT等多维度数据。滑动窗口是**统计引擎**，一个LeapArray同时服务限流/熔断/系统保护三种规则。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 滑动窗口更精确 |
| 高级开发 | 滑动窗口比固定窗口没有2倍突发问题，实现简单 |
| 架构师 | Sentinel选滑动窗口的根本原因是多维度统计。令牌桶/漏桶只有单维度（令牌数），无法支撑熔断器的异常比例、慢调用比例判断。滑动窗口是统计引擎，一个LeapArray服务限流/熔断/系统保护三种规则。两者不是替代而是配合：排队等待模式下Sentinel用漏桶效果做匀速 |

### 怎么补
1. **记住核心句**："令牌桶统计不了RT和异常率，但熔断器需要这些数据"
2. **理解LeapArray结构**：环形数组 + 窗口过期重置 + LongAdder高并发统计，面试时能说出这个结构
3. **配合关系**：滑动窗口负责统计+判断，排队等待模式用漏桶做匀速效果，不是替代

---

## 差距五：降级策略缺决策框架
> Day2发现

### 现状
能说出四种降级策略的适用场景，但是罗列式的，不是二维决策框架。选"异步化"时没有说明决策路径。

### 架构师为什么需要
罗列=背书，二维矩阵=决策。面试官想听的是"你怎么选的"，不是"你知道几种"。二维矩阵（实时性×数据过期容忍度）体现的是结构化决策能力。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 降级返回默认值 |
| 高级开发 | 非核心用兜底值，核心用备链路 |
| 架构师 | 降级策略是二维矩阵：实时性×数据过期容忍度。兜底值(低实时+可过期)、缓存(高实时+可过期)、异步化(低实时+不可过期)、备链路(高实时+不可过期)。核心链路绝不用兜底值——宁可慢也不能骗用户。秒杀订单降级走异步化+返回排队凭证，因为允许延迟但不允许假数据 |

### 怎么补
1. **记住二维矩阵**：实时性(高/低) × 数据过期(允许/不允许) = 四种策略
2. **每遇到降级场景，先定位到矩阵的哪个象限**，再选策略
3. **面试话术**："核心链路降级先看两件事：用户要不要立刻看到结果？数据能不能接受过期？"

---

## 差距六：隔离策略缺超时意识
> Day2发现

### 现状
答"第三方支付API怎么做隔离"时说了策略+工厂+线程池隔离，实战经验对。但没提到超时控制——**没有超时的隔离只是"不拖Tomcat"，线程池资源还是会被耗尽**。

### 架构师为什么需要
线程池隔离的核心不是"分开线程"，而是"可控超时"。面试官问隔离，你答线程池，追问"超时怎么设"，答不上就白答了。CompletableFuture.orTimeout(3, SECONDS) 才是隔离的灵魂。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 调第三方用线程池隔离 |
| 高级开发 | 第三方调用用独立线程池，防止拖垮主线程池 |
| 架构师 | 线程池隔离的灵魂是超时控制，不是独立线程。CompletableFuture+独立线程池+orTimeout(3s)三位一体。没有超时的隔离只是"不拖Tomcat"，线程池资源还是会被耗尽。内部服务用Sentinel信号量+熔断（轻量够用），外部服务自己做CompletableFuture+线程池+超时（彻底隔离） |

### 怎么补
1. **记住核心句**："超时是隔离的灵魂"
2. **写一段代码**：CompletableFuture.supplyAsync(外部调用, externalPool).orTimeout(3, SECONDS)，写一遍就记住了
3. **面试时主动提**："我们第三方调用是线程池隔离+3秒超时+降级三件套"

---

## 差距七：数字直觉仍不足
> Day2发现，延续第4周差距一

### 现状
答容错时全程无量级数字——网关限流多少QPS？熔断阈值多少？售罄后DB压力降多少？都没有说。

### 第4周已识别，第5周仍未改善

| 缺失数字 | 应该说出的 |
|---------|-----------|
| 网关限流 | 令牌桶1万QPS |
| 单用户限流 | 滑动窗口5 QPS |
| 熔断阈值 | 慢调用比例50%，RT 500ms，timeWindow 10s |
| minRequestAmount | 5（防误杀） |
| 售罄后DB压力 | 归零（从5秒卖完到活动结束） |
| 信号量隔离并发数 | 线程数限流50 |
| 线程数vs QPS公式 | 并发线程数 ≈ QPS × RT |

### 怎么补
1. **每组规则配置带数字**：写熔断规则时不只写策略，带上count/timeWindow/minRequestAmount
2. **回答时先给数字再解释**："网关限流1万QPS，因为压测单机承载上限是1.2万"
3. **建立数字直觉**：不需要精确，但要有量级——"万级QPS""毫秒级RT""秒级熔断"

---

## 本周自查清单（Day1-Day2）

每答完一道题，过一遍：

- [ ] **数字**：有没有量级数字？QPS/RT/阈值/窗口时间
- [ ] **漏斗**：能不能画出每层挡多少的定量结构？
- [ ] **决策框架**：是罗列还是矩阵？选型有没有"为什么选这个、为什么不选那个"
- [ ] **故障模式匹配**：每种策略/算法对应什么故障场景？
- [ ] **超时意识**：提到隔离时，有没有说超时控制？

## 差距八：Distro协议理解停留在口号
> Day3发现

### 现状
答"读全量写部分"方向对但太模糊，说不出Hash分区→写负责节点→异步同步的完整流程。面试官追问"写请求打到非负责节点怎么办"就答不上来。

### 架构师为什么需要
注册中心是微服务基础设施的基石，Distro是Nacos AP模式的核心协议。面试官问"Nacos AP模式怎么保证最终一致"，答"读全量写部分"像背书，答不出Hash分区+fire-and-forget异步同步就没有深度。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | Nacos支持AP模式 |
| 高级开发 | Distro协议，读全量写部分 |
| 架构师 | Distro协议本质是Hash分区+异步复制：每个节点负责一部分Service（hashCode%nodeCount），写请求打到任意节点→转发到负责节点→本地写+fire-and-forget异步同步其他节点，读请求任意节点直接返回本地全量数据。新节点加入走全量同步，后续走增量同步，定期校验数据一致性做兜底 |

### 怎么补
1. **画Distro写流程图**：客户端→任意节点N1→判断负责→本地写→异步同步→其他节点更新
2. **和Eureka对比**：Eureka写扩散（同步N-1次阻塞），Distro fire-and-forget（异步不阻塞）
3. **面试话术**："Distro的核心是职责划分，每个节点只负责一部分service的写入，其他节点通过异步同步获得全量数据"

---

## 差距九：临时vs持久实例搞混核心区别
> Day3发现

### 现状
关注了通信协议（gRPC/HTTP），但漏了核心——谁做健康检查。把注册中心的UDP推拉和配置中心的Long Polling混为一谈。

### 架构师为什么需要
临时vs持久实例是Nacos面试必考点，核心区别不是协议而是健康检查发起方。搞混注册中心和配置中心的通信模型说明没有理解"数据特性决定技术选型"。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 临时实例AP，持久实例CP |
| 高级开发 | 临时实例客户端心跳，持久实例服务端探测 |
| 架构师 | 核心区别是健康检查发起方：临时实例由客户端心跳上报（5s/15s不健康/30s剔除），持久实例由服务端主动探测（只标记不剔除）。注册中心推拉结合因UDP不可靠需拉取兜底，配置中心Long Polling因配置不能丢——两个场景的通信模型由数据特性决定，不是随意选择 |

### 怎么补
1. **记住核心句**："临时实例客户端证明自己活着，持久实例服务端验证对方活着"
2. **两套通信模型分开记**：注册中心=UDP推送+6秒拉取兜底，配置中心=Long Polling 29.5秒
3. **面试时主动说选型根因**："注册数据允许短暂不一致所以用轻量UDP，配置数据不能丢所以用可靠Long Polling"

---

## 差距十：配置灰度和回滚缺实操方案
> Day3发现

### 现状
知道配置中心可以热更新，但灰度发布和回滚的具体方案说不出。不知道Nacos Beta发布功能，不知道回滚时正在运行的实例怎么处理。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | Nacos改配置不用重启 |
| 高级开发 | 支持灰度发布和回滚 |
| 架构师 | 配置灰度推荐按集群灰度+Nacos Beta发布（指定灰度IP），逐步扩大范围。回滚靠配置历史+Long Polling秒级推送，正在执行的请求用旧配置——秒级生效已足够。防配置事故是五层防御：审批→灰度→回滚→校验→监控 |

### 怎么补
1. **实操一次**：Nacos控制台做一次Beta发布→观察→全量发布→回滚
2. **写SOP**：配置变更SOP，含灰度步骤和回滚步骤
3. **记住"五层防御"**：审批→灰度→回滚→校验→监控，和Day2降级三层架构思维一脉相承

---

## 差距十一：链路追踪的跨场景传播不清晰
> Day3发现

### 现状
知道Trace/Span模型和HTTP Header传播，但跨线程（TransmittableThreadLocal）和跨MQ（Message Property）的传播方案不了解。采样策略只知概念不知选型。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 用SkyWalking做链路追踪 |
| 高级开发 | TraceId通过Header传播 |
| 架构师 | 三种传播场景三种方案：HTTP→Header（W3C标准）、MQ→Message Property、线程池→TransmittableThreadLocal。架构师要封装统一SDK让开发无感知。采样选头部+尾部组合：头部1%覆盖正常流量，尾部100%保留异常，成本几乎不增但排障价值大增 |

### 怎么补
1. **写三种传播的伪代码**：HTTP Header / MQ Property / TTL，各写一遍就记住了
2. **理解"开发无感知"**：封装TraceUtils.asyncRun()和TraceMessage.wrap()，开发只需调工具类
3. **采样组合**：记住"正常1%+异常100%"，面试时能解释为什么纯头部采样会漏异常

---

## 差距十二：超时重试缺联动思维
> Day3发现

### 现状
能分别说超时、重试、熔断，但三者之间的联动关系不清晰。不知道"熔断策略决定重试策略"，不知道超时嵌套原则。

### 对比

| 层次 | 回答 |
|------|------|
| 开发 | 配个超时3秒，重试2次 |
| 高级开发 | 非幂等不重试，重试要指数退避 |
| 架构师 | 超时+重试+熔断是铁三角：熔断OPEN→不超时不重试直接降级，熔断策略决定重试策略（异常数熔断→不重试），超时嵌套原则（外层>内层之和，留降级余量）。重试三大坑：非幂等数据重复/重试风暴雪崩/超时叠加用户等太久 |

### 怎么补
1. **画铁三角流程图**：熔断状态→决定是否重试→超时预算分配
2. **记住联动句**："熔断策略决定重试策略——异常数熔断不重试，慢调用比例熔断可以重试换实例"
3. **写三层超时配置**：网关30s > 服务10s > 下游3s×2次=6s，服务层有4秒降级余量

---

## 本周自查清单（Day1-Day3）

每答完一道题，过一遍：

- [ ] **数字**：有没有量级数字？QPS/RT/阈值/窗口时间
- [ ] **漏斗**：能不能画出每层挡多少的定量结构？
- [ ] **决策框架**：是罗列还是矩阵？选型有没有"为什么选这个、为什么不选那个"
- [ ] **故障模式匹配**：每种策略/算法对应什么故障场景？
- [ ] **超时意识**：提到隔离/重试时，有没有说超时控制？
- [ ] **数据特性→技术选型**：能不能说清"为什么这个场景选这个方案"的根因？
- [ ] **联动思维**：多个机制（超时+重试+熔断，推+拉，灰度+回滚）是独立配置还是联动系统？

### 一句话总结

**Day3的核心差距是"数据特性驱动选型"的意识缺失：注册中心为什么用UDP不用WebSocket？配置中心为什么用Long Polling不用推送？采样为什么头部+尾部组合？每个技术选型背后都有数据特性驱动，架构师要能说清"为什么"而不是"是什么"。**

---

*日期：2026-05-27 | 第5周 Day3 | 能力差距梳理*
