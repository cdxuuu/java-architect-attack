# 架构师学习-能力差距梳理（2026年07月第3周）

> 周主题：K8s 与云原生专题
>
> 按主题组织，每个差距标注发现日期。跨周延续的差距标注来源。
>
> 累计差距延续：截至第2周末累计 60+ 项，本周在此基础上继续累积。

---

## 一、K8s 核心架构与控制面（Day01）

### 差距1.1：K8s 控制面组件实战调优不足
> Day1发现

- **现状**：知道 K8s 控制面组件构成（API Server/etcd/Scheduler/Controller Manager），但 API Server、etcd 的性能调优（max-requests-inflight、APF PriorityLevel、etcd –quota-backend-bytes）只调过参数，未深度实战
- **架构师水平**：能根据集群规模（节点数、Pod 数、QPS）规划 API Server/etcd 资源，能基于监控指标（apiserver_request_duration、etcd_wal_fsync_duration）做容量评估，能设计 APF FlowSchema/PriorityLevel 分级限流
- **补足方向**：
  - 精读 K8s 官方"Scaling Kubernetes to 2,500 Nodes"系列博客
  - 学习 APF FlowSchema/PriorityLevel 配置，在生产集群验证限流效果
  - 复盘啄木鸟云健康集群（如有）的 API Server 监控指标

### 差距1.2：etcd 备份恢复与故障演练缺失
> Day1发现

- **现状**：知道 etcd 是 K8s 命根子，但没做过 etcd 备份恢复演练、脑裂演练
- **架构师水平**：能设计 etcd 备份策略（snapshot 周期 + 异地备份 + 恢复演练），能在 etcd 脑裂时快速诊断与恢复
- **补足方向**：
  - 搭 3 节点 etcd 集群，做脑裂、磁盘满、节点宕机演练
  - 学习 `etcdctl snapshot save/restore` 完整流程
  - 在生产集群配置 etcd 监控告警（fsync 延迟、leader 切换、磁盘使用率）

### 差距1.3：Leader 选举机制细节不熟
> Day1发现

- **现状**：知道 kube-scheduler / kube-controller-manager 用 Leader 选举，但基于 Lease 资源的细节（holderIdentity / leaseDuration / renewTime / acquireTime）不熟
- **架构师水平**：能讲清 Lease 资源的字段含义、续约机制、切换时间窗口（默认 5-15s），能自定义控制器复用同样的 Leader 选举机制（client-go 的 leader-election 库）
- **补足方向**：
  - 阅读 client-go leader-election 库源码
  - 自定义控制器实践 Leader 选举
  - 学习 K8s 1.20+ 的 Lease 升级（coordination.k8s.io/v1）

### 差距1.4：API Server 故障排查体系化不足
> Day1发现，延续第2周差距2.3（长连接网关水平扩展）

- **现状**：知道 API Server 慢会影响集群，但 API Server 慢的诊断（429 告警、慢请求分析、watch 滞后）不熟
- **架构师水平**：能基于 apiserver_request_duration、apiserver_storage_objects、workqueue_depth 等指标定位 API Server 瓶颈，能区分 etcd I/O / 准入 webhook / List 请求三类瓶颈
- **补足方向**：
  - 学习 K8s 控制面监控指标体系（apiserver_*、etcd_*、workqueue_*）
  - 学习 API Server audit log 分析方法
  - 复盘啄木鸟云健康集群（如有）的 API Server 历史 incidents

---

## 二、Pod 模型与多容器设计（Day01）

### 差距2.1：Sidecar 模式实战主要在 Istio Envoy，自定义 Sidecar 经验不足
> Day1发现

- **现状**：用过 Istio Envoy、Filebeat Sidecar，但对 Ambassador / Adapter 模式、自研 Sidecar 注入控制器不熟
- **架构师水平**：能根据业务需求设计多容器 Pod（如医疗 IM 长连接网关 + Mesh Sidecar + 日志 Sidecar），能自研 MutatingAdmissionWebhook 实现 Sidecar 自动注入
- **补足方向**：
  - 阅读 Istio Sidecar Injector 实现源码
  - 自研一个简单的 Sidecar 注入控制器（如自动注入日志采集容器）
  - 复盘啄木鸟云健康线上 Sidecar 模式使用情况，识别可优化点

### 差距2.2：探针配置缺乏统一规范
> Day1发现

- **现状**：探针配置主要复制粘贴，liveness / readiness / startup 没有清晰职责划分
- **架构师水平**：能为不同类型应用（Java/Go/Python、Web/Worker/CronJob）制定探针配置规范，能基于 Actuator/Micrometer 设计精细化健康检查
- **补足方向**：
  - 制定 Spring Boot 应用探针配置规范（liveness + readiness + startup 分离）
  - 制定 Go/Python 应用探针配置规范
  - 在啄木鸟云健康线上推广，逐步替换老配置

### 差距2.3：多容器 Pod 资源竞争调优经验不足
> Day1发现

- **现状**：知道多容器 Pod 共享 network namespace，但 CPU/IO/网络资源竞争的调优（CPU Manager static 策略、cgroup v2 io 限制）不熟
- **架构师水平**：能为多容器 Pod 设计精细化的资源配额（业务容器独占 CPU、Sidecar 限制 burst），能基于 cgroup v2 配置 IO 隔离避免日志 Sidecar 拖慢主容器
- **补足方向**：
  - 学习 K8s CPU Manager（static 策略）、Memory Manager
  - 学习 cgroup v2 io 隔离配置
  - 在测试集群验证 Sidecar 资源配额对主容器的影响

### 差距2.4：K8s 故障排查体系化不足
> Day1发现，延续第5周差距2.3（微服务长连接网关）

- **现状**：能查 `kubectl describe`、`kubectl logs`，但 CrashLoopBackOff、OOMKilled、Init:Error 等高频故障的体系化排查不熟
- **架构师水平**：能建立 K8s 故障排查手册（按现象 -> 原因 -> 诊断 -> 处理流程化），能基于 kubelet/容器运行时日志深入分析
- **补足方向**：
  - 整理 K8s 常见故障案例库（CrashLoopBackOff、ImagePullBackOff、OOMKilled、Pending、Evicted）
  - 学习 `crictl`、`ctr`、`kubectl debug` 等工具
  - 复盘历史故障（如 Pod 频繁重启、节点 NotReady）形成 SOP

### 差距2.5：K8s 1.28+ sidecarContainers 新特性不熟
> Day1发现

- **现状**：知道 K8s 1.28 引入了 sidecarContainers（Init Container + restartPolicy: Always），但生产实战没用过
- **架构师水平**：能在新版本集群中用 sidecarContainers 解决"Sidecar 必须先启动、Sidecar 随主容器退出"问题，能平滑迁移老的 Sidecar 配置
- **补足方向**：
  - 在测试集群验证 sidecarContainers 行为
  - 学习从老 Sidecar 模式迁移到 sidecarContainers 的最佳实践

---

## 三、与往周专题的衔接差距（Day01）

### 差距3.1：K8s 与微服务专题的衔接认知不深
> Day1发现，延续第5周差距（微服务）

- **现状**：知道 K8s 提供 DNS + ConfigMap + Ingress + Mesh，但与 Nacos/Sentinel/Seata 等独立组件的取舍不清
- **架构师水平**：能讲清"K8s 原生能力 vs 独立中间件"的边界（如 K8s Service 替代 Nacos 服务发现，K8s ConfigMap 替代 Nacos 配置中心，但 Sentinel 限流仍需独立组件或下沉到 Mesh），能根据业务阶段（小规模 vs 大规模）选择合适方案
- **补足方向**：
  - 对比 K8s ConfigMap vs Nacos 配置中心（动态推送、灰度发布、多环境）
  - 对比 K8s Service vs Nacos 服务发现（DNS vs RPC 注册、跨集群）
  - 学习 Sentinel 与 Istio Envoy 的限流能力对比

### 差距3.2：K8s 与限流降级专题的衔接认知不深
> Day1发现，延续第4周差距（限流降级）

- **现状**：知道限流可下沉到 Service Mesh，但 Sentinel vs Istio Envoy 的限流能力差异、生产实践不熟
- **架构师水平**：能讲清"应用层限流（Sentinel）vs 网关层限流（Nginx/APISIX）vs Mesh 层限流（Envoy）"三层限流的分工，能根据 SLA 与故障域设计多层级流
- **补足方向**：
  - 学习 Istio Envoy 的限流机制（Global Rate Limit + Local Rate Limit）
  - 学习 Sentinel 的"集群限流 + Token Server"与 Envoy 的"全局限流 + Redis"对比
  - 设计多层级限流方案（Mesh + 网关 + 应用）

---

## 四、Workload 编排与发布工程（Day02）

### 差距4.1：StatefulSet 生产实战与 Operator 模式经验不足
> Day2发现

- **现状**：知道 StatefulSet 三大特性（稳定标识/稳定存储/有序部署），但 Redis Cluster、Kafka 等有状态服务的 Operator 模式（如 Redis Operator、Strimzi Kafka Operator）没用过，生产部署还是手写 StatefulSet YAML
- **架构师水平**：能基于 Operator 管理有状态服务生命周期（扩缩、滚动、备份恢复、故障转移），能自研简单 Operator（如医疗 IM 长连接网关的状态管理）
- **补足方向**：
  - 研读 Redis Operator / Strimzi Kafka Operator 源码
  - 学习 Operator SDK / Kubebuilder，自研一个简单的 Operator
  - 在啄木鸟云健康测试集群验证 Redis Operator 部署 Redis Cluster

### 差距4.2：Indexed Job 工程化使用经验不足
> Day2发现

- **现状**：知道 Indexed Job 是 K8s 1.24 GA，但公司批处理任务大多还是用 XXL-Job 或自研调度器
- **架构师水平**：能根据批处理场景（数据量、单任务耗时、并行度）选择合适方案（Indexed Job vs Job+工作队列 vs XXL-Job vs Airflow），能设计分片策略与失败重试机制
- **补足方向**：
  - 在啄木鸟云健康找一个批处理场景（如体检报告 PDF 批量生成）用 Indexed Job 重构
  - 对比 XXL-Job 的优劣（任务可视化、分片机制、失败告警）
  - 学习 K8s 1.27+ 的 Job Pod Failure Policy 新特性

### 差距4.3：DaemonSet 大规模集群滚动节奏控制经验不足
> Day2发现

- **现状**：知道 DaemonSet 每节点一份，但大规模集群（100+ 节点）的滚动节奏（maxUnavailable 百分比、节点分批、PDB 配合）经验不足
- **架构师水平**：能为 100+ 节点集群设计 DaemonSet 滚动方案（按节点池分批、PDB 保护、监控告警），能在不影响业务情况下完成日志/监控 Agent 升级
- **补足方向**：
  - 学习 DaemonSet 的 maxUnavailable 百分比配置
  - 研究节点池（NodePool）+ DaemonSet 的分批滚动
  - 学习 PodDisruptionBudget 在 DaemonSet 滚动中的应用

### 差距4.4：金丝雀发布工程化实战不足
> Day2发现，延续第5周差距2.3（微服务）

- **现状**：公司大多还是简单滚动发布，金丝雀发布停留在"Istio VirtualService weight 改一下"的手动操作，没有完整的金丝雀流水线（自动切流量、自动监控、自动回滚）
- **架构师水平**：能基于 Argo Rollouts 或自研控制器设计完整金丝雀流水线（5% -> 50% -> 100%，每阶段自动验证指标、自动回滚），能集成 Prometheus/SkyWalking 做指标驱动的发布决策
- **补足方向**：
  - 学习 Argo Rollouts 的 AnalysisTemplate 与 metric-based 回滚
  - 在啄木鸟云健康测试集群搭建 Argo Rollouts + Istio 金丝雀流水线
  - 研究基于业务指标（下单成功率）的金丝雀验证，而非只看 HTTP 5xx

### 差距4.5：HPA + 滚动发布雪崩场景没有压测验证
> Day2发现

- **现状**：知道 HPA + 滚动发布可能雪崩，但没有压测验证过雪崩场景，防御方案（发布期间禁用 HPA 缩容）只是理论
- **架构师水平**：能用 Chaos Mesh 做"HPA + 滚动发布 + 流量突增"混沌实验，验证雪崩场景与防御方案，能基于压测结果制定发布期间 HPA 行为策略
- **补足方向**：
  - 在测试集群用 Chaos Mesh + k6 做 HPA 雪崩压测
  - 验证 HPA scaleDown.stabilizationWindowSeconds 对雪崩的防御效果
  - 研究业务低峰期发布窗口的选取策略

### 差距4.6：Spring Boot 优雅停机与 K8s preStop 配合不深
> Day2发现，延续第5周差距2.3（微服务）

- **现状**：知道 K8s preStop + terminationGracePeriodSeconds，但 Spring Boot 2.3+ 的 graceful shutdown 配置不熟，发布期间偶有请求丢失
- **架构师水平**：能设计零请求丢失的优雅停机方案（preStop sleep + Spring Boot graceful shutdown + readinessProbe 摘流量 + 自定义关闭钩子），能基于压测验证发布期间零请求丢失
- **补足方向**：
  - 学习 Spring Boot 2.3+ 的 `server.shutdown.grace-period` 配置
  - 在啄木鸟云健康测试集群压测发布期间请求丢失率
  - 制定 Spring Boot 应用优雅停机配置规范

### 差距4.7：GitOps 发布与回滚机制不熟
> Day2发现

- **现状**：公司发布大多是 `kubectl apply` 手动操作或 CI/CD 流水线直接 apply，没有用 GitOps 工具
- **架构师水平**：能基于 ArgoCD/Flux 设计 GitOps 发布流程（Git PR -> 自动同步 -> 多环境 rollout），能用 Git 历史做回滚（Git revert 触发 ArgoCD 同步）
- **补足方向**：
  - 在测试集群搭建 ArgoCD，把现有 K8s YAML 接入 GitOps
  - 学习 ArgoCD 的 ApplicationSet 多环境发布
  - 对比 ArgoCD 回滚 vs K8s 原生 `rollout undo` 的差异

### 差距4.8：Workload 与往周专题的衔接认知不深
> Day2发现，延续第5周差距2.3（微服务）

- **现状**：K8s Workload 与往周专题（Redis Cluster 部署、Kafka 部署、医疗 IM 长连接）的衔接认知不深
- **架构师水平**：能讲清"K8s 原生 Workload vs 中间件 Operator"的边界（K8s StatefulSet 提供基础，Operator 提供业务生命周期管理），能根据业务阶段选择合适方案
- **补足方向**：
  - 对比 K8s 原生 StatefulSet 部署 Redis vs Redis Operator
  - 学习 Strimzi Kafka Operator 的 CRD 设计
  - 研究医疗 IM 长连接网关在 K8s 上的 Workload 选型（StatefulSet + 稳定标识 vs Deployment + SessionAffinity）

---

## 五、Service 与 kube-proxy 转发原理（Day03）

### 差距5.1：kube-proxy iptables vs ipvs 实战调优不足
> Day3发现

- **现状**：知道 iptables 和 ipvs 两种模式，但没做过规则数与性能的量化对比，没切过 ipvs 模式
- **架构师水平**：能根据集群规模（Service/Endpoint 数）选模式，能基于 `iptables-save | wc -l` 和 `ipvsadm -L -n | wc -l` 输出做规则诊断，能在大集群平滑切换 ipvs（含 KPR kube-proxy replacement）
- **补足方向**：
  - 搭大集群（1000+ Service）压测对比 iptables vs ipvs 的 CPU/延迟
  - 学习 ipvs 调度算法（rr/wrr/lc/sh/sed）的适用场景
  - 研究 KPR（kube-proxy replacement）方案，用 Cilium 替代 kube-proxy

### 差距5.2：CoreDNS 解析链路与 ndots:5 陷阱不熟
> Day3发现

- **现状**：知道 CoreDNS 解析 Service 域名，但 ndots:5 陷阱、NodeLocal DNSCache、CoreDNS Plugin 机制不熟
- **架构师水平**：能基于 DNS 查询日志诊断解析慢的根因，能调优 ndots / cache TTL / CoreDNS 副本数，能设计 NodeLocal DNSCache 部署架构
- **补足方向**：
  - 精读 CoreDNS 官方 Plugin 文档（kubernetes / forward / cache / rewrite）
  - 在生产集群部署 NodeLocal DNSCache 对比命中率
  - 复盘啄木鸟云健康 DNS 慢的线上案例（如有），用 CoreDNS 查询日志定位

### 差距5.3：保留客户端 IP 的方案不全面
> Day3发现

- **现状**：用过 `externalTrafficPolicy: Local`，但对 PROXY Protocol、Cilium eBPF 直连等方案不熟
- **架构师水平**：能根据业务场景（医保审计、风控、限流按 IP）选最合适的客户端 IP 保留方案，能权衡流量分布不均的代价
- **补足方向**：
  - 学习 nginx-ingress PROXY Protocol 配置
  - 研究 Cilium eBPF 直连保留源 IP 方案
  - 复盘啄木鸟云健康医保接口审计日志场景，确认源 IP 保留是否合规

### 差距5.4：EndpointSlice 大集群优化实战不深
> Day3发现

- **现状**：知道 EndpointSlice 取代 Endpoints，但没在大集群验证过 watch 流量优化效果
- **架构师水平**：能基于 `maxEndpointsPerSlice` 参数调优切片大小，能用 metrics 量化 watch 流量从 MB 降到 KB 的效果，能用 `topologyAwareHints` 优化跨 AZ 流量
- **补足方向**：
  - 学习 EndpointSlice 的 topologyHints 机制
  - 在测试集群对比 Endpoints vs EndpointSlice 的 watch 流量
  - 研究 topologyAwareHints 与 `externalTrafficPolicy: Local` 的配合

---

## 六、Ingress / CNI / DNS / NetworkPolicy（Day03）

### 差距6.1：Ingress Controller 选型与 Gateway API 不熟
> Day3发现

- **现状**：只用过 nginx-ingress，对 APISIX / Istio Gateway / Gateway API 选型差异不熟
- **架构师水平**：能根据业务场景（API 网关 / 传统 Web / Service Mesh）选 Ingress Controller，能用 Gateway API 设计跨 Controller 兼容的网络方案
- **补足方向**：
  - 精读 Gateway API 官方文档（GatewayClass / Gateway / HTTPRoute 三层模型）
  - 对比 APASIX 与 nginx-ingress 的插件能力矩阵
  - 在新集群尝试 Gateway API 替代 Ingress

### 差距6.2：Cilium eBPF 只有概念认知
> Day3发现，延续第2周差距2.1（Service Mesh Sidecar）

- **现状**：知道 eBPF 是趋势，但没用过 Cilium，对 KPR、Hubble、L7 NetworkPolicy 不熟
- **架构师水平**：能选型 Cilium 替代 Flannel/Calico，能启用 KPR 替代 kube-proxy，能用 Hubble 做流量观测，能用 Cilium L7 NetworkPolicy 做细粒度限流
- **补足方向**：
  - 搭 Cilium 集群做对比测试（vs Calico iptables 模式）
  - 学习 eBPF 基础（bcc / bpftrace）
  - 研究 Cilium Service Mesh（无 Sidecar 模式）

### 差距6.3：NetworkPolicy 多租户隔离不熟
> Day3发现

- **现状**：写过简单 NetworkPolicy，但 default-deny-all + 白名单放行的多租户隔离策略不熟
- **架构师水平**：能为多医院场景设计 Namespace + NetworkPolicy + RBAC + Istio AuthorizationPolicy 四层隔离方案
- **补足方向**：
  - 学习 Cilium NetworkPolicy（L7 增强）的写法
  - 研究 Istio AuthorizationPolicy 的 L7 授权机制
  - 制定啄木鸟云健康多医院隔离 SOP

### 差距6.4：Service Mesh 与 K8s Service 的协作关系不清晰
> Day3发现，延续第2周差距2.1（Service Mesh Sidecar）

- **现状**：用过 Istio VirtualService，但与 K8s Service 的分层关系（寻址 vs 路由）不清晰
- **架构师水平**：能讲清"K8s Service 提供寻址 + Istio VirtualService 提供路由"的分层，能用 Istio 做灰度/蓝绿/故障隔离
- **补足方向**：
  - 精读 Istio 流量治理模型（VirtualService / DestinationRule / Gateway）
  - 研究 Istio mTLS 与 K8s NetworkPolicy 的协作
  - 复盘啄木鸟云健康灰度发布场景，设计 Istio + K8s Service 配合方案

### 差距6.5：多租户网络架构整体设计经验不足
> Day3发现

- **现状**：能配单个 NetworkPolicy，但"南北向 + 东西向 + DNS + Service Mesh + 安全合规"五位一体的整体网络架构设计经验不足
- **架构师水平**：能为多医院平台设计完整网络架构（WAF + LB + Ingress + CNI + DNS + Istio + 审计），能权衡性能、隔离、合规、可观测性多个维度
- **补足方向**：
  - 学习大型 K8s 多租户平台网络架构案例（如阿里云 ACK / 华为云 CCE）
  - 设计啄木鸟云健康多医院网络架构方案（含 Namespace 划分、NetworkPolicy 矩阵、DNS 配置、Istio 衔接）
  - 研究医疗等保 2.0 合规要求在网络架构中的落地

---

## 七、PV/PVC/StorageClass 与 CSI 存储体系（Day04）

### 差距7.1：CSI 三大组件调用链路实战不足
> Day4发现

- **现状**：知道 CSI 是 K8s 存储驱动标准，但 CSI Identity / Controller / Node 三大组件的调用链路（CreateVolume / ControllerPublishVolume / NodeStageVolume / NodePublishVolume）只停留在概念
- **架构师水平**：能部署 CSI 驱动（如阿里云 CSI / Ceph CSI），能用 `crictl logs` / `kubectl get csidriver` 诊断 CSI 问题，能基于 CSI 接口调试存储异常
- **补足方向**：
  - 在测试集群部署 Ceph CSI / 阿里云 CSI，跟踪 CreateVolume 全链路日志
  - 研究 CSI Spec 官方文档
  - 复盘啄木鸟云健康线上存储异常案例（如有），用 CSI 日志定位

### 差距7.2：StorageClass 跨 AZ 调度实战不深
> Day4发现

- **现状**：用过 StorageClass，但 `volumeBindingMode: WaitForFirstConsumer` 在跨 AZ 场景的实战不深
- **架构师水平**：能在多 AZ 集群对比 Immediate vs WaitForFirstConsumer 的 PV 创建位置，能设计跨 AZ 容灾的 StorageClass
- **补足方向**：
  - 在多 AZ 集群验证 WaitForFirstConsumer 的拓扑感知
  - 研究跨 AZ PV 的故障切换机制
  - 设计 MySQL 主从跨 AZ 容灾的 StorageClass 方案

### 差距7.3：Redis/MySQL Operator 模式实战不足
> Day4发现，延续第2周差距4.1（StatefulSet + Operator）

- **现状**：有状态服务用过手写 StatefulSet + PVC，没用过 Operator 模式
- **架构师水平**：能用 Redis Operator / Oracle MySQL Operator / ECK 部署生产有状态服务，能自研简单 Operator（如医疗 IM 长连接网关的状态管理）
- **补足方向**：
  - 研究 Redis Operator / Oracle MySQL Operator 的 CRD 设计
  - 学习 Operator SDK / Kubebuilder 自研 Operator
  - 在测试集群用 Redis Operator 部署 Redis Cluster

### 差距7.4：reclaimPolicy 与生产数据保护策略不熟
> Day4发现

- **现状**：用过 Retain 和 Delete，但生产场景的"Retain + 备份 + 跨 Region 复制 + DR 演练"完整数据保护策略不熟
- **架构师水平**：能根据业务 SLA 设计多级数据保护方案（RPO/RTO 量化），能制定误删恢复 SOP
- **补足方向**：
  - 学习 Velero + Restic 的 PV 备份恢复
  - 研究 Operator 的备份 CRD（Redis Operator backup / MySQL Operator backup）
  - 设计啄木鸟云健康数据保护 SOP

---

## 八、ConfigMap/Secret 与有状态服务存储设计（Day04）

### 差距8.1：ConfigMap 与 Apollo 配置中心边界不清晰
> Day4发现，延续第5周差距3.1（K8s 与微服务衔接）

- **现状**：用过 ConfigMap 和 Apollo，但二者边界不清晰（什么配置放 ConfigMap，什么放 Apollo）
- **架构师水平**：能制定"启动配置 ConfigMap + 业务配置 Apollo"的协同方案，能用 Spring Cloud Kubernetes 实现配置热更新
- **补足方向**：
  - 对比 ConfigMap 与 Apollo 的能力矩阵（热更新、灰度发布、多环境）
  - 制定啄木鸟云健康配置管理规范
  - 研究 Spring Cloud Kubernetes 的 @RefreshScope

### 差距8.2：Secret 加密只用了 base64，没开过 KMS
> Day4发现

- **现状**：Secret 默认 base64 编码，没开过 etcd 静态加密 / KMS
- **架构师水平**：能配置 EncryptionConfiguration 启用 etcd 静态加密，能集成外部 KMS（阿里云 KMS / HashiCorp Vault），能用 External Secrets Operator 实现凭证轮换
- **补足方向**：
  - 在测试集群配置 EncryptionConfiguration
  - 学习 External Secrets Operator 的部署与使用
  - 研究 Sealed Secrets 的 GitOps 实践

### 差距8.3：SubPath 陷阱与 ConfigMap 热更新机制不熟
> Day4发现

- **现状**：用过 ConfigMap Volume 挂载，但 SubPath 不会热更新的陷阱不清楚，热更新机制（kubelet 周期同步 + symlink 替换）不熟
- **架构师水平**：能讲清 ConfigMap Volume 挂载的热更新链路，能用 Reloader / Spring Cloud Kubernetes / Apollo 解决热更新问题
- **补足方向**：
  - 研究 kubelet Volume Mount 的 symlink 机制
  - 部署 Reloader 测试自动重启
  - 学习 Spring Cloud Kubernetes 的 @RefreshScope

### 差距8.4：医疗影像 DICOM 存储设计缺乏
> Day4发现

- **现状**：知道 DICOM 是医疗影像协议，但对象存储 + DICOM Web + 冷热分层 + WORM 合规的完整方案没设计过
- **架构师水平**：能为医疗影像设计 OSS + 生命周期规则 + WORM + KMS 加密 + 跨 Region 复制的完整方案，能集成 Orthanc / dcm4che DICOM Web 网关
- **补足方向**：
  - 学习阿里云 OSS 生命周期规则配置
  - 研究 Orthanc / dcm4che 部署
  - 学习 DICOM Web 协议（QIDO/WADO/STOW）

### 差距8.5：跨集群数据迁移与 DR 演练只有理论
> Day4发现

- **现状**：知道 Velero 备份恢复，但没做过完整的跨集群 DR 演练
- **架构师水平**：能用 Velero + Operator 备份 CRD 设计完整的 DR 方案，能量化 RTO / RPO，能定期做 DR 演练
- **补足方向**：
  - 用 Velero 在测试集群做 DR 演练
  - 学习 Operator 的备份恢复 CRD
  - 研究跨 Region 数据复制与切换流程

### 差距8.6：有状态服务 Operator 选型与 CRD 设计经验不足
> Day4发现，延续第2周差距4.1（StatefulSet + Operator）

- **现状**：知道有状态服务推荐用 Operator，但 Redis Operator / Strimzi Kafka Operator / ECK / Oracle MySQL Operator 的选型差异、CRD 设计不熟
- **架构师水平**：能根据业务规模（小/中/大）选 Operator，能读懂 Operator CRD 字段含义，能自研简单 Operator
- **补足方向**：
  - 对比主流 Operator 的能力矩阵（备份/恢复/升级/扩缩容/故障切换）
  - 学习 Operator SDK / Kubebuilder 自研 Operator
  - 研究啄木鸟云健康有状态服务的 Operator 选型方案

### 差距8.7：存储成本优化与冷热分层实战不足
> Day4发现

- **现状**：用过 ESSD / 对象存储，但冷热分层（hot/warm/cold/archive）的成本优化方案只在概念层
- **架构师水平**：能根据数据访问频率设计冷热分层方案（30 天 hot / 90 天 warm / 180 天 cold / 5 年 archive），能量化成本节省，能落地生命周期规则
- **补足方向**：
  - 学习阿里云 OSS / AWS S3 的生命周期规则配置
  - 设计啄木鸟云健康体检报告 PDF 与医疗影像 DICOM 的冷热分层方案
  - 量化对比全 Hot vs 分层的成本差异

---